[
  {
    "verdict": "needs_validation",
    "fingerprint": "auspicios/grabar/unvalidated-body-to-privileged-kbs",
    "title": "Authenticated auspicios caller can relay an unvalidated body to the privileged KBServicios saveauspicio endpoint",
    "description": "POST /api/auspicios/grabar requires a session plus the auspicios:view permission (middleware.ts:29 and route.ts:9-10), then forwards the caller-supplied JSON verbatim to the legacy KBServicios endpoint with the server-side x-api-key (route.ts:13-18). The sibling /api/auspicios/listar constrains the body to code/codeEvent (listar/route.ts:13-14), showing the authoring intent that the downstream contract is narrow, but grabar applies no schema. Whether the unchecked residual fields let a non-privileged caller change privileged auspicio state cannot be decided from the repository because the downstream /rest/saveauspicio contract is absent; the candidate therefore remains unvalidated.",
    "claimed_root_cause": "grabar/route.ts parses the body with no Zod/DTO allowlist and passes JSON.stringify(body) unmodified to `${KBS_URL}/rest/saveauspicio` under the server-side KBSERVICIOS_API_KEY, unlike listar which validates code/codeEvent.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 29,
        "scope": "PROTECTED entry for /api/auspicios",
        "description": "Requests to /api/auspicios require authentication and the auspicios:view permission, so the caller is an authenticated internal-area user rather than anonymous."
      },
      {
        "kind": "propagation",
        "file": "src/app/api/auspicios/grabar/route.ts",
        "line": 13,
        "scope": "POST handler body parsing",
        "description": "The raw request.json() value is taken with no schema parse or field allowlist."
      },
      {
        "kind": "sink",
        "file": "src/app/api/auspicios/grabar/route.ts",
        "line": 17,
        "scope": "outbound fetch to KBServicios",
        "description": "JSON.stringify(body) is posted to ${KBS_URL}/rest/saveauspicio with the server-side x-api-key header, so all caller-controlled keys reach the privileged downstream service."
      }
    ],
    "evidence": [
      {
        "file": "src/app/api/auspicios/grabar/route.ts",
        "line": 6,
        "description": "API_KEY is read from process.env.KBSERVICIOS_API_KEY and injected as x-api-key on the outbound call."
      },
      {
        "file": "src/app/api/auspicios/grabar/route.ts",
        "line": 13,
        "description": "const body = await request.json() with no validation before the downstream call."
      },
      {
        "file": "src/app/api/auspicios/listar/route.ts",
        "line": 14,
        "description": "The sibling listar endpoint requires code and codeEvent, evidencing that the downstream auspicio contract is field-scoped while grabar is not."
      },
      {
        "file": "src/middleware.ts",
        "line": 29,
        "description": "The strongest source-visible control is session + auspicios:view; no per-field or role-scope restriction exists."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "The downstream /rest/saveauspicio contract (accepted fields and which of them change privileged state beyond the caller's own auspicio) is not present anywhere in the repository, so the boundary violation cannot be established from source alone.",
      "Whether AUSPICIOS_API_URL/KBSERVICIOS_API_KEY are configured and what authorization KBServicios applies to unexpected fields are deployment facts."
    ],
    "validation_plan": {
      "deployment": "An owner or operator of the KBServicios service confirms, from the /rest/saveauspicio contract or service configuration, which fields the endpoint accepts, whether unrecognized keys are ignored, and which accepted fields can change privileged auspicio state beyond the caller own auspicio; this is a documentation/configuration review and no request is sent to the running service."
    }
  },
  {
    "verdict": "needs_validation",
    "fingerprint": "auth.jwt.fallback-secret",
    "title": "JWT signed/verified with a hardcoded fallback secret when JWT_SECRET is unset",
    "description": "The signing/verifying module falls back to the constant \"dev-secret-cambiar-en-produccion\" (auth.ts:9) and the event-selection path falls back to a different constant \"dev-secret\" (auth-service.ts:53). If the deployment does not set JWT_SECRET, tokens are signed with a publicly known key. .env.prod.example ships JWT_SECRET empty while Dockerfile.ecs bakes only a build-time placeholder, so whether the fallback is live is a deployment fact.",
    "claimed_root_cause": "Hardcoded fallback secrets for HS256 signing/verification, plus a divergent fallback in seleccionarEvento, with no startup assertion that JWT_SECRET is configured.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/auth/[...slug]/route.ts",
        "line": 10,
        "scope": "POST /api/auth/login",
        "description": "Issues a signed session token via signToken."
      },
      {
        "kind": "propagation",
        "file": "src/lib/server/auth.ts",
        "line": 9,
        "scope": "SECRET initialization",
        "description": "Falls back to a hardcoded constant when process.env.JWT_SECRET is undefined."
      },
      {
        "kind": "sink",
        "file": "src/lib/server/auth.ts",
        "line": 41,
        "scope": "signToken",
        "description": "Signs (and verifyToken verifies) with the resolved secret."
      }
    ],
    "evidence": [
      {
        "file": "src/lib/server/auth.ts",
        "line": 9,
        "description": "Hardcoded fallback secret used for all HS256 signing/verification."
      },
      {
        "file": "src/application/auth/auth-service.ts",
        "line": 53,
        "description": "Divergent fallback secret \"dev-secret\" in seleccionarEvento's jwtVerify."
      },
      {
        "file": ".env.prod.example",
        "line": 29,
        "description": "JWT_SECRET is shipped empty, so a missing value is plausible."
      },
      {
        "file": "Dockerfile.ecs",
        "line": 37,
        "description": "Bakes only a build-time placeholder JWT_SECRET."
      },
      {
        "file": "docs/01-despliegue/arquitectura-aws.md",
        "line": 268,
        "description": "Documents JWT_SECRET as coming from Secrets Manager, i.e. the real value is a deployment fact."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "the deployed JWT_SECRET value (and whether it is actually configured) is a deployment fact not visible in repository source"
    ],
    "validation_plan": {
      "local": "In a controlled environment with an OS-enforced sandbox, launch the app with JWT_SECRET unset and unset/empty .env.prod, then sign a token with the hardcoded fallback constant and replay it against a protected route to confirm the fallback is used; do not execute target code on an unsandboxed host.",
      "deployment": "An owner inspects the ECS task definition or the resolved docker-compose .env.prod to confirm whether JWT_SECRET is set to a non-empty, strong value distinct from the Dockerfile.ecs build-time placeholder and the hardcoded fallback; this is a configuration inspection only, with no token presented to the running service."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "auth.login.plaintext-password-storage",
    "title": "Passwords stored and compared in plaintext",
    "description": "Login verifies the submitted password with a direct string equality against a cleartext password column, and accounts are created/seeded with cleartext values (fixed default \"123456\"). No password hashing (Argon2/bcrypt/scrypt/PBKDF2) or constant-time comparison exists anywhere in src, so any read of the user_role table yields reusable credentials.",
    "root_cause": "The UserRole.password column stores cleartext and AuthApplicationService.login compares it with `!==`; AuthPrismaRepository.updatePassword and prisma/seed.ts write cleartext, and no hashing utility exists in the codebase.",
    "intended_behavior": "Passwords must be salted, one-way hashed, and verified with a constant-time comparison; password changes and resets must re-hash.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/auth/[...slug]/route.ts",
        "line": 10,
        "scope": "POST /api/auth/login",
        "description": "Slug router dispatches the login action to authController.login."
      },
      {
        "kind": "propagation",
        "file": "src/application/auth/auth-service.ts",
        "line": 23,
        "scope": "AuthApplicationService.login",
        "description": "Compares userRoles[0].password !== dto.password against the stored value."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/auth-repository.ts",
        "line": 8,
        "scope": "AuthPrismaRepository.findByEmail",
        "description": "Selects the cleartext password column used for the comparison."
      }
    ],
    "evidence": [
      {
        "file": "src/application/auth/auth-service.ts",
        "line": 23,
        "description": "Plaintext equality comparison of the supplied password."
      },
      {
        "file": "prisma/schema.prisma",
        "line": 125,
        "description": "password String @default(\"123456\") @db.VarChar(100) — cleartext default credential."
      },
      {
        "file": "prisma/seed.ts",
        "line": 49,
        "description": "Test users are created with cleartext passwords such as admin123/cliente123."
      },
      {
        "file": "src/infrastructure/persistence/auth-repository.ts",
        "line": 79,
        "description": "updatePassword writes the new password verbatim (no hashing)."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "The login endpoint is reachable anonymously (public /api/auth/ prefix)."
      },
      {
        "kind": "data_state",
        "description": "At least one account row exists; seeded and account-creation flows use the cleartext default 123456."
      }
    ],
    "execution": {
      "attacker_perspective": "An anonymous attacker who obtains any copy of the user_role table (DB read, backup, SQL injection elsewhere, error dump) or a legitimate account holder.",
      "payloads": [
        "POST /api/auth/login {\"email\":\"cliente@iimp.org.pe\",\"password\":\"cliente123\"}"
      ],
      "instructions": [
        "Read the password column value for a target account from user_role.",
        "Submit it verbatim to POST /api/auth/login."
      ],
      "observed_result": "Source analysis shows the login code performs an exact string comparison against the stored value, so the stored value is directly usable as the password, and the seeded default 123456 is valid for accounts whose password was never changed."
    },
    "remediation": {
      "strategy": "Introduce a password hashing utility (Argon2id or bcrypt) and use it on account creation, profile/password updates and seed data; verify with a constant-time hash comparison and migrate/force-reset existing cleartext credentials."
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "Plaintext storage plus a fixed default makes credential recovery trivial for anyone who reaches the stored data or guesses the default."
      },
      "impact": {
        "score": "high",
        "reason": "Full account takeover for any harvested row, including the seeded admin, and password-reuse exposure for all users; exploitation requires a read of the cleartext column, so the demonstrated impact does not meet the critical anchor (no takeover is established from source alone)."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "Storage, comparison, seed and update paths all read directly from source with no hashing layer anywhere in src."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "auth.perfil.update-mass-assignment",
    "title": "Profile self-update mass-assigns arbitrary UserRole columns",
    "description": "PATCH /api/auth/perfil binds the write to the session email but forwards the raw JSON body, typed only at compile time as PerfilUpdateRequestDTO, into prisma.userRole.updateMany. Any additional JSON keys that exist on the UserRole model (userId, roleId, email, password, resetToken, resetTokenExpires) are written, enabling self privilege escalation and identity manipulation.",
    "root_cause": "authController.updatePerfil assigns await request.json() to a PerfilUpdateRequestDTO with no runtime validation, AuthApplicationService passes it through, and AuthPrismaRepository.updatePerfil runs updateMany({ where: { email }, data }) with the attacker-controlled object; Prisma's UncheckedUpdateManyInput accepts any valid UserRole column.",
    "intended_behavior": "A profile self-update must validate the body against a server-side allowlist and write only profile fields; identity, credential and authorization columns must never be reachable from the request.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/auth/[...slug]/route.ts",
        "line": 17,
        "scope": "PATCH /api/auth/perfil",
        "description": "Slug router dispatches the profile update to authController.updatePerfil."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/auth.controller.ts",
        "line": 64,
        "scope": "authController.updatePerfil",
        "description": "Assigns await request.json() to PerfilUpdateRequestDTO with no Zod/runtime parse."
      },
      {
        "kind": "propagation",
        "file": "src/application/auth/auth-service.ts",
        "line": 91,
        "scope": "AuthApplicationService.updatePerfil",
        "description": "Forwards the object unchanged to repo.updatePerfil(session.email, dto)."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/auth-repository.ts",
        "line": 57,
        "scope": "AuthPrismaRepository.updatePerfil",
        "description": "prisma.userRole.updateMany({ where: { email }, data }) with the unvalidated object."
      }
    ],
    "evidence": [
      {
        "file": "src/types/dto/auth/perfil-update-request.dto.ts",
        "line": 1,
        "description": "PerfilUpdateRequestDTO is a compile-time interface with no runtime enforcement."
      },
      {
        "file": "src/controllers/auth.controller.ts",
        "line": 64,
        "description": "Raw JSON cast to the DTO; no parse or field allowlist."
      },
      {
        "file": "src/infrastructure/persistence/auth-repository.ts",
        "line": 57,
        "description": "updateMany spreads the incoming object as Prisma data."
      },
      {
        "file": "prisma/schema.prisma",
        "line": 124,
        "description": "UserRole exposes email, password (line 125), roleId (line 123), resetToken/resetTokenExpires (lines 132-133) — all writable columns."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "Any authenticated role, including cliente, can call PATCH /api/auth/perfil."
      },
      {
        "kind": "data_state",
        "description": "Escalation to a privileged role requires knowing a role id; identity takeover requires a target email."
      }
    ],
    "execution": {
      "attacker_perspective": "An authenticated low-privilege user (e.g. cliente) updating their own profile.",
      "payloads": [
        "PATCH /api/auth/perfil {\"nombre\":\"x\",\"roleId\":\"<admin-role-uuid>\"}",
        "PATCH /api/auth/perfil {\"email\":\"victim@iimp.org.pe\",\"password\":\"chosen-password\"}"
      ],
      "instructions": [
        "Authenticate and obtain the session cookie.",
        "Send PATCH /api/auth/perfil with DTO fields plus extra UserRole columns such as roleId, password or email.",
        "Reuse the escalated session or log in with the altered identity."
      ],
      "observed_result": "Source analysis shows the unvalidated body reaches updateMany without an allowlist, so model fields supplied beyond the documented DTO are persisted onto the caller's own UserRole row, including roleId/password/userId/email."
    },
    "remediation": {
      "strategy": "Parse the request with a strict Zod allowlist (only nombre/apellidos/telefono/tipoUsuarioId/idEmpresa/nombreEmpresa), build an explicit data object from validated fields, and never forward the raw body to Prisma; treat role/credential columns as server-only."
    },
    "severity": {
      "likelihood": {
        "score": "medium",
        "reason": "Any authenticated user can attempt it, though escalating to admin needs knowledge of a role id."
      },
      "impact": {
        "score": "high",
        "reason": "Writing roleId/password/userId/email on the caller's row enables privilege escalation and account-identity manipulation."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "Entry point, cast, service passthrough, repository updateMany and the writable UserRole columns are all directly visible in source."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "auth.reset-token.math-random",
    "title": "Password reset token generated with Math.random()",
    "description": "The password-reset token is built from Math.random(), a non-cryptographic PRNG, and mailed in a URL query string. Although the token is 32 characters, expiring and single-use, its values are not drawn from a CSPRNG.",
    "root_cause": "AuthApplicationService.requestReset builds the token as [...Array(32)].map(() => Math.random().toString(36)[2]).join(\"\") and persists/mails it; no crypto.randomBytes/randomUUID is used.",
    "intended_behavior": "Recovery tokens must be generated from a cryptographically secure random source, expiring and single-use.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/auth/[...slug]/route.ts",
        "line": 13,
        "scope": "POST /api/auth/reset-password",
        "description": "Dispatches the reset request to authController.requestReset."
      },
      {
        "kind": "propagation",
        "file": "src/application/auth/auth-service.ts",
        "line": 99,
        "scope": "AuthApplicationService.requestReset",
        "description": "Generates the reset token with Math.random()."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/auth-repository.ts",
        "line": 68,
        "scope": "AuthPrismaRepository.setResetToken",
        "description": "Persists the token and its 30-minute expiry on the user row."
      }
    ],
    "evidence": [
      {
        "file": "src/application/auth/auth-service.ts",
        "line": 99,
        "description": "Math.random()-derived 32-character reset token."
      },
      {
        "file": "src/application/auth/auth-service.ts",
        "line": 102,
        "description": "Token embedded in the emailed reset URL as a query parameter."
      },
      {
        "file": "src/infrastructure/persistence/auth-repository.ts",
        "line": 73,
        "description": "Token accepted only while resetTokenExpires >= now (30-minute window)."
      },
      {
        "file": "src/infrastructure/persistence/auth-repository.ts",
        "line": 79,
        "description": "Token cleared after a successful reset (single-use)."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "The reset-request endpoint is anonymously reachable."
      },
      {
        "kind": "timing_dependency",
        "description": "The token is valid for a 30-minute window and can be used once."
      }
    ],
    "execution": {
      "attacker_perspective": "An anonymous attacker who can trigger a reset for a victim email and then interact with the reset URL, or who can predict the PRNG output.",
      "payloads": [
        "POST /api/auth/reset-password {\"email\":\"victim@iimp.org.pe\"}"
      ],
      "instructions": [
        "Request a password reset for the victim account.",
        "Derive or guess the non-CSPRNG token within the 30-minute validity window.",
        "Submit POST /api/auth/reset-password/confirm with the token and a chosen password."
      ],
      "observed_result": "Source analysis confirms the token is 32 base36 characters produced by Math.random(), which is not cryptographically secure, so token values are predictable in principle within their validity window."
    },
    "remediation": {
      "strategy": "Replace Math.random() with a CSPRNG (crypto.randomBytes/randomUUID) and store only a hash of the reset token."
    },
    "severity": {
      "likelihood": {
        "score": "medium",
        "reason": "Exploitation requires predicting/observing the PRNG output within a short window, but the source provides no CSPRNG guarantee."
      },
      "impact": {
        "score": "high",
        "reason": "A guessed or predicted reset token allows setting a new password and taking over the account."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "Token generation, persistence, expiry and single-use behavior are all directly readable in source."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "authz.jwt.claims-only-no-revocation",
    "title": "Authorization trusts JWT claims with no revocation until 24h expiry",
    "description": "Middleware and hasPermission authorize solely from signed JWT claims. Permission/role changes in the database (roles.controller -> role-repository.updatePermisos/addUser/removeUser) do not invalidate issued tokens, and hasDBPermission (the only DB-backed check) is called at a single endpoint. A revoked or demoted principal keeps access for up to the 24h token lifetime.",
    "root_cause": "The middleware gate calls hasPermission(payload, permission), which reads payload.permissions/payload.roles only; there is no denylist, session version or app-wide DB permission check, and tokens expire only after 24h.",
    "intended_behavior": "Authorization must reflect current server-side role/permission state, or revocation must take effect within a bounded short window.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 58,
        "scope": "middleware PROTECTED prefix loop",
        "description": "Rejects only when hasPermission(payload, route.permission) is false, using claims from the token."
      },
      {
        "kind": "propagation",
        "file": "src/lib/server/auth.ts",
        "line": 70,
        "scope": "hasPermission",
        "description": "Reads payload.permissions and payload.roles from the JWT, with no database lookup."
      },
      {
        "kind": "sink",
        "file": "src/lib/server/auth.ts",
        "line": 40,
        "scope": "signToken",
        "description": "Issues a 24h token whose claims cannot be revoked before expiry."
      }
    ],
    "evidence": [
      {
        "file": "src/lib/server/auth.ts",
        "line": 70,
        "description": "hasPermission returns payload.permissions.includes(...) || payload.roles.includes(admin)."
      },
      {
        "file": "src/middleware.ts",
        "line": 58,
        "description": "The authorization gate consumes only the verified claims."
      },
      {
        "file": "src/lib/server/auth.ts",
        "line": 40,
        "description": "Tokens are set to expire after 24h with no shorter revalidation window."
      },
      {
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 54,
        "description": "hasDBPermission is imported and used at exactly this one endpoint; no other route re-checks the database."
      },
      {
        "file": "src/infrastructure/persistence/role-repository.ts",
        "line": 29,
        "description": "Role permissions are mutated in the database independently of issued tokens."
      }
    ],
    "conditions": [
      {
        "kind": "authorization_role",
        "description": "An administrator changes permissions (updatePermisos/addUser/removeUser) for the principal."
      },
      {
        "kind": "timing_dependency",
        "description": "The affected principal keeps using the token within its 24h lifetime."
      }
    ],
    "execution": {
      "attacker_perspective": "A user whose permission or role was just revoked, or a terminated account, still holding a valid session cookie.",
      "payloads": [
        "GET /api/facturacion/... with the previously issued token cookie"
      ],
      "instructions": [
        "Obtain a token while holding a permission.",
        "Have an administrator revoke the permission or remove the account.",
        "Continue calling routes protected by that permission with the same cookie."
      ],
      "observed_result": "Source analysis shows the gate verifies only the signature and reads permissions from the token, so revoked permissions remain effective until the 24h expiry."
    },
    "remediation": {
      "strategy": "Use short-lived access tokens with refresh plus a server-side session/token version or denylist checked on each request, and apply DB-backed permission checks to sensitive routes rather than trusting claims alone."
    },
    "severity": {
      "likelihood": {
        "score": "medium",
        "reason": "Requires a prior legitimate grant and a subsequent role/permission change, which is a normal administrative operation."
      },
      "impact": {
        "score": "high",
        "reason": "Revoked or demoted principals retain full access to protected functionality for up to 24h."
      },
      "overall_severity": "medium"
    },
    "confidence": {
      "score": "high",
      "reason": "The claims-only gate, the single hasDBPermission call site and the 24h lifetime are all directly confirmed from source."
    }
  },
  {
    "verdict": "rejected",
    "fingerprint": "components.plano.documentos.unvalidated-scheme-to-href",
    "title": "Stored documentos URLs reach anchor href without scheme validation, but React neutralizes executable schemes",
    "description": "The documentos arrays accepted by gess.validator.ts and reserva.validator.ts are unconstrained strings and are rendered as anchor hrefs, but the framework sanitizer replaces javascript: URLs with a throwing URL, so no executable script sink survives; data: top-level navigation is browser-blocked and target=_blank implies noopener. The missing allowlist is a hardening gap, not a demonstrated vulnerability.",
    "claimed_root_cause": "No URL scheme allowlist at the write validators (gess.validator.ts:6, reserva.validator.ts:5) and no scheme check at the React href renders (plano-dinamico.tsx:649, plano-isometrico.tsx:619, reservas-manager.tsx:216, solicitudes-manager.tsx:586).",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/validators/gess.validator.ts",
        "line": 6,
        "scope": "updateGessStandSchema.documentos",
        "description": "documentos: z.array(z.string()).optional() and imagenes accept any string; written by gessController.actualizar (gess.controller.ts:33-37) and gess-service.sync."
      },
      {
        "kind": "propagation",
        "file": "src/application/reservas/reserva-service.ts",
        "line": 41,
        "scope": "crear solicitud",
        "description": "request.documentos from reserva.validator.ts:5 is persisted into the solicitud and later returned to the UI."
      },
      {
        "kind": "sink",
        "file": "src/components/plano/plano-dinamico.tsx",
        "line": 649,
        "scope": "detail modal documentos",
        "description": "<a href={url} target=_blank> renders the stored string; plano-isometrico.tsx:619, reservas-manager.tsx:216 and solicitudes-manager.tsx:586 are identical."
      }
    ],
    "evidence": [
      {
        "file": "node_modules/react-dom/cjs/react-dom-client.production.js",
        "line": 13004,
        "description": "href/src branch (12989-13005) calls sanitizeURL before setAttribute."
      },
      {
        "file": "node_modules/react-dom/cjs/react-dom-client.production.js",
        "line": 1412,
        "description": "sanitizeURL (1412-1414) plus isJavaScriptProtocol rewrite javascript: URLs to a throwing URL."
      },
      {
        "file": "src/components/solicitudes/solicitudes-manager.tsx",
        "line": 586,
        "description": "The same unvalidated url is interpolated only as React text (url.split('/').pop()), which React escapes."
      }
    ],
    "reason": "React 19.2.4's sanitizer is a real, source-verified control that blocks the javascript: scheme on href/src, the only executable sink on these paths; data: cannot top-level navigate in modern browsers and target=_blank implies noopener, so no attacker-controlled script execution or session-bearing disclosure remains. The residual gap (no http/https allowlist) has no affected executing principal and is recorded as hardening."
  },
  {
    "verdict": "confirmed",
    "fingerprint": "docker-compose.prod.yml:root-container-rw-binds-host-source",
    "title": "Production container runs as root and read-write bind-mounts the host source tree",
    "description": "The production image declares no USER, supervisord runs as root, and docker-compose.prod.yml bind-mounts `.:/app` read-write. A compromise of the internet-facing Next.js/nginx processes therefore yields root within the container and write access to the host repository source, enabling code tampering that affects subsequent deploys.",
    "root_cause": "No non-root user is set for the runtime image, and the application directory is a read-write bind mount of the host working tree rather than an immutable, read-only artifact.",
    "intended_behavior": "Run the workload as a dedicated unprivileged UID and mount application code read-only (or bake it into the image) so container compromise cannot modify host source.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "Dockerfile",
        "line": 6,
        "scope": "runtime image",
        "description": "FROM node:20-bookworm-slim with no USER directive, so the container process runs as root."
      },
      {
        "kind": "propagation",
        "file": "docker/supervisord.conf",
        "line": 3,
        "scope": "supervisord",
        "description": "user=root supervises node and nginx, all running as root."
      },
      {
        "kind": "sink",
        "file": "docker-compose.prod.yml",
        "line": 52,
        "scope": "app service volumes",
        "description": "- .:/app mounts the host repository read-write into the root container."
      }
    ],
    "evidence": [
      {
        "file": "Dockerfile",
        "line": 55,
        "description": "ENTRYPOINT into a root image that installs nginx/supervisor and never drops privileges."
      },
      {
        "file": "docker/supervisord.conf",
        "line": 3,
        "description": "user=root for the supervisor controlling the app and nginx."
      },
      {
        "file": "docker-compose.prod.yml",
        "line": 52,
        "description": ".:/app — default read-write bind of the host working tree (only /etc/letsencrypt is flagged :ro)."
      }
    ],
    "conditions": [
      {
        "kind": "system_configuration",
        "description": "Applies to the EC2 Docker Compose deployment; the ECS image (Dockerfile.ecs) instead runs as USER nextjs with baked-in code."
      }
    ],
    "execution": {
      "attacker_perspective": "Remote attacker who achieves code execution in the Next.js server or nginx process inside the production container.",
      "payloads": [
        "id  # -> uid=0(root)",
        "echo 'malicious' >> /app/src/<any-file>  # writes the host working tree"
      ],
      "instructions": [
        "Static reproduction (no target execution performed): confirm Dockerfile has no USER, supervisord.conf:3 sets user=root, and docker-compose.prod.yml:52 mounts .:/app without :ro.",
        "Deployment-time reproduction: after accessing the container, run `id` and write to a file under /app, then observe the change on the host working tree."
      ],
      "observed_result": "Source establishes root execution and a read-write host bind; container compromise can modify the host repository source used by future deployments."
    },
    "remediation": {
      "strategy": "Add a non-root user to the Dockerfile, run supervisord/programs as that user, and mount the source read-only (or bake code into the image).",
      "code_changes": [
        {
          "file_name": "docker-compose.prod.yml",
          "fixed_code": "    volumes:\n      - .:/app:ro\n      - /etc/letsencrypt:/etc/letsencrypt:ro"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "medium",
        "reason": "Depends on a separate container-compromise primitive, but the app is internet-facing and runs many third-party dependencies."
      },
      "impact": {
        "score": "high",
        "reason": "Root in the container plus a read-write host source mount allows tampering with application code and persistence across restarts and deploys."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "Absence of USER, user=root, and the read-write bind mount are all explicit in the cited source."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "docker.entrypoint.sh:prisma-db-push-in-production-startup",
    "title": "Production container forces schema with prisma db push on every startup",
    "description": "docker/entrypoint.sh executes `npx prisma db push --skip-generate` inside the wait-for-PostgreSQL loop whenever DATABASE_URL is set, before gated `prisma migrate deploy`. In the EC2/Compose production path (Dockerfile ENTRYPOINT entrypoint.sh plus .env.prod) this reconciles the live database to schema.prisma on every container start, bypassing reviewed migrations and able to alter or drop schema/data without a migration record.",
    "root_cause": "`prisma db push` is used for a connectivity wait and is not gated by the migration flags; only `prisma migrate deploy` is gated by FIRST_RUN/RUN_MIGRATIONS.",
    "intended_behavior": "Production schema changes should occur only through reviewed migrations (`prisma migrate deploy`); `db push` must never run against a production database.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "docker/entrypoint.sh",
        "line": 21,
        "scope": "wait-for-PostgreSQL block",
        "description": "if [ -n \"${DATABASE_URL}\" ] — the db push loop is entered whenever DATABASE_URL is present."
      },
      {
        "kind": "propagation",
        "file": "docker/entrypoint.sh",
        "line": 24,
        "scope": "startup loop (30 iterations)",
        "description": "npx prisma db push --skip-generate executes on every startup/retry."
      },
      {
        "kind": "sink",
        "file": "docker/entrypoint.sh",
        "line": 38,
        "scope": "gated migrations",
        "description": "prisma migrate deploy runs only when FIRST_RUN or RUN_MIGRATIONS=true, so db push can change schema before any reviewed migration applies."
      }
    ],
    "evidence": [
      {
        "file": "docker/entrypoint.sh",
        "line": 24,
        "description": "Unconditional `db push` inside the wait loop, capped only by 30 iterations, not by any environment flag."
      },
      {
        "file": "Dockerfile",
        "line": 55,
        "description": "ENTRYPOINT points at docker/entrypoint.sh, so this runs in the production Docker/Compose image."
      },
      {
        "file": "docker-compose.prod.yml",
        "line": 40,
        "description": "Production app service loads .env.prod, which supplies DATABASE_URL and NODE_ENV=production."
      },
      {
        "file": ".env.prod.example",
        "line": 20,
        "description": "DATABASE_URL is a required production setting, so the db push branch is always entered."
      }
    ],
    "conditions": [
      {
        "kind": "data_state",
        "description": "Impact on data depends on live schema drift: an incompatible schema.prisma can cause db push to alter or drop columns/values without a migration record."
      },
      {
        "kind": "environmental_dependency",
        "description": "Applies to the EC2 Docker Compose deployment using Dockerfile + docker/entrypoint.sh; the ECS path uses Dockerfile.ecs + docker/entrypoint-ecs.sh."
      }
    ],
    "execution": {
      "attacker_perspective": "Not attacker-triggered; any container restart or redeploy (or an operator pushing a schema change) executes the statement against production data.",
      "payloads": [
        "npx prisma db push --skip-generate (as run by docker/entrypoint.sh:24)"
      ],
      "instructions": [
        "Static reproduction (no target execution performed): read docker/entrypoint.sh lines 21-38 and confirm db push is inside the DATABASE_URL branch while migrate deploy is gated separately.",
        "Deployment-time reproduction: start the production container with DATABASE_URL set and observe prisma db push running on the live database."
      ],
      "observed_result": "Source shows db push executes on every production startup regardless of RUN_MIGRATIONS; the schema is force-synchronized to the deployed schema.prisma outside the migration history."
    },
    "remediation": {
      "strategy": "Remove prisma db push from the production entrypoint; gate all schema mutation behind reviewed `prisma migrate deploy`.",
      "code_changes": [
        {
          "file_name": "docker/entrypoint.sh",
          "fixed_code": "if [ -n \"${DATABASE_URL}\" ]; then\n    echo \"⏳  Esperando PostgreSQL...\"\n    for i in $(seq 1 30); do\n        if npx prisma db execute --stdin <<< 'SELECT 1' >/dev/null 2>&1; then\n            echo \"✅  PostgreSQL listo.\"\n            break\n        fi\n        sleep 2\n    done\nfi"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "Runs on every production container start with DATABASE_URL present; no flag prevents it."
      },
      "impact": {
        "score": "high",
        "reason": "db push can perform destructive schema/data changes outside migration review and record, risking production data loss and divergence from the migration history."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "The call site and its ungated condition are explicit in source; only the magnitude depends on live schema drift."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "email/templates/unescaped-html-interpolation",
    "title": "Unescaped attacker input interpolated into HTML notification emails",
    "description": "The reservation flow accepts unauthenticated, unbounded string fields (razonSocial, tipoDocumento, numeroDocumento) and interpolates them verbatim into HTML email bodies. The admin notification built by buildAdminNotificacionEmail embeds them into a table cell and the subject, and the client confirmation email does the same. No HTML escaping or output encoding is applied anywhere between the request and the Resend `html` field, so an anonymous submitter can inject markup into the email that IIMP administrators receive, enabling spoofed content and links inside a message that appears to originate from the platform.",
    "root_cause": "buildReservaConfirmationEmail and buildAdminNotificacionEmail in src/lib/server/email.ts concatenate request-derived strings directly into HTML template literals without escaping, and buildRevisionEmail in src/lib/server/email-templates.ts interpolates opts.mensaje the same way; no escaping helper is applied on the path.",
    "intended_behavior": "Any value derived from a caller should be HTML-encoded (or assembled through a context-aware template that escapes) before it is placed in an HTML email body or subject, so injected markup stays inert text.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/reservas/[...slug]/route.ts",
        "line": 6,
        "scope": "POST /api/reservas/crear dispatch",
        "description": "createRouter maps the anonymous POST to reservaController.crear; the route is not in PROTECTED, so an unauthenticated caller reaches it."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/reserva.controller.ts",
        "line": 12,
        "scope": "reservaRequestSchema.parse(raw)",
        "description": "The caller body is parsed by reservaRequestSchema, whose razonSocial/tipoDocumento/numeroDocumento are unbounded z.string() values copied into the reserva request."
      },
      {
        "kind": "propagation",
        "file": "src/application/reservas/reserva-service.ts",
        "line": 95,
        "scope": "emailData construction",
        "description": "request.datos.razonSocial and the composed documento string are placed into emailData and passed to both email builders."
      },
      {
        "kind": "sink",
        "file": "src/lib/server/email.ts",
        "line": 144,
        "scope": "buildReservaConfirmationEmail HTML table cell",
        "description": "emailData.razonSocial is concatenated into an HTML table cell; lines 143 and 145 also interpolate standCodes and documento, and the admin builder repeats the same raw values."
      }
    ],
    "evidence": [
      {
        "file": "src/lib/server/email.ts",
        "line": 144,
        "description": "Razon social interpolated raw into the confirmation HTML body."
      },
      {
        "file": "src/lib/server/email.ts",
        "line": 178,
        "description": "Admin notification interpolates the same attacker-supplied razonSocial raw."
      },
      {
        "file": "src/lib/server/email.ts",
        "line": 180,
        "description": "Attacker-supplied emailCliente interpolated raw into the admin email."
      },
      {
        "file": "src/application/reservas/reserva-service.ts",
        "line": 108,
        "description": "The admin notification is sent to ADMIN_EMAIL when it differs from the contact address (reserva-service.ts:106-108)."
      },
      {
        "file": "src/validators/reserva.validator.ts",
        "line": 6,
        "description": "razonSocial/tipoDocumento/numeroDocumento accepted as unbounded strings."
      },
      {
        "file": "src/lib/server/email-templates.ts",
        "line": 22,
        "description": "opts.mensaje interpolated raw in the personalizado template (reviewer-supplied path)."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "anonymous: /api/reservas is not in the middleware PROTECTED list, so no session is required."
      },
      {
        "kind": "user_interaction",
        "description": "an administrator must open the delivered notification email for the injected markup to render."
      },
      {
        "kind": "third_party_dependency",
        "description": "delivery depends on RESEND_API_KEY being configured; the HTML is submitted to Resend as-is."
      }
    ],
    "execution": {
      "attacker_perspective": "Unauthenticated network client submitting a stand reservation.",
      "payloads": [
        "POST /api/reservas/crear body {\"standIds\":[\"<existing-stand-id>\"],\"datos\":{\"razonSocial\":\"<b>IIMP</b><a href=\\\"https://evil.example\\\">Ver contrato</a>\",\"tipoDocumento\":\"DNI\",\"numeroDocumento\":\"12345678\",\"email\":\"attacker@example.com\"}} with no Cookie header"
      ],
      "instructions": [
        "Send POST /api/reservas/crear with the payload above and no token cookie.",
        "Observe that reservaRequestSchema.parse accepts the markup as a plain string and that no escaping is applied.",
        "Follow the value from reserva-service.ts:95 into buildAdminNotificacionEmail and into the html field handed to Resend; it appears inside the HTML table cell unencoded."
      ],
      "observed_result": "Not executed (no OS-enforced sandbox on the audit host); the source trace deterministically shows the attacker-controlled razonSocial and documento strings are concatenated unescaped into the HTML body sent to ADMIN_EMAIL, so the admin notification contains attacker markup rather than inert text."
    },
    "remediation": {
      "strategy": "HTML-escape every caller-derived value at the interpolation point (one escapeHtml helper used by buildReservaConfirmationEmail, buildAdminNotificacionEmail, and buildRevisionEmail), or move the templates to a context-aware auto-escaping renderer; add a regression test asserting that '<' is emitted as &lt; in the admin body."
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "the reservation endpoint is unauthenticated and the injected fields have no length or charset constraints, so a single request is sufficient."
      },
      "impact": {
        "score": "medium",
        "reason": "attacker-controlled HTML and links render inside a notification email recipients trust as platform-originated; impact is content and link spoofing (phishing), not code execution."
      },
      "overall_severity": "medium"
    },
    "confidence": {
      "score": "high",
      "reason": "the interpolation sites and the absence of any escaping helper are directly visible at the cited lines."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "entidades/persona/pii-search-any-authenticated-role",
    "title": "Any authenticated role can run arbitrary person-PII searches against the upstream person-search service",
    "description": "The middleware protects /api/entidades by authentication only, with no permission field (middleware.ts:30), so every authenticated role including `cliente` (constants.ts:212) may call POST /api/entidades/persona. The controller takes an arbitrary documento or nombre and forwards it unmodified to the upstream person-search endpoint (entidades.controller.ts:7-18, entidades-client.ts:28-33), returning the full PersonaResultDTO record set (documento, nombres, fecha de nacimiento, correo, celular, direccion, etc., entidades.dto.ts:13-36). No ownership, scope or rate binding exists, so a low-trust authenticated user can enumerate PII for any natural person.",
    "root_cause": "The /api/entidades prefix is registered in middleware.ts:30 without a `permission`, and entidadesController.searchPerson performs no getSession/authorization check, passing attacker-controlled documento/nombre straight to the upstream PII service and returning the raw response.",
    "intended_behavior": "A person-PII lookup should require an explicit permission that binds the caller to the resource, or at minimum be limited to roles whose function needs it, instead of being available to every authenticated role including customers.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 30,
        "scope": "PROTECTED entry for /api/entidades",
        "description": "The route prefix is listed with no permission, so the middleware gate is satisfied by any valid session token."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/entidades.controller.ts",
        "line": 13,
        "scope": "entidadesController.searchPerson",
        "description": "The controller passes the attacker-supplied body directly to the upstream client with no permission or scope check."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/external/entidades-client.ts",
        "line": 29,
        "scope": "POST /rest/searchpersonv00",
        "description": "The server-side request is issued to the legacy person-search service and its full PII list is returned to the caller."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 30,
        "description": "{ path: \"/api/entidades\" } has no permission, unlike /api/solicitudes, /api/facturacion, etc."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 212,
        "description": "The lowest-trust `cliente` role holds only eventos:datos, solicitudes:view, stands:plano, read/write:reservas, confirming entidades search is reachable by customers."
      },
      {
        "file": "src/types/dto/entidades/entidades.dto.ts",
        "line": 13,
        "description": "PersonaResultDTO lists the PII returned: documento, nombres/apellidos, fecha_nacimiento, correo, celular, direccion, empresa."
      },
      {
        "file": "src/infrastructure/external/entidades-client.ts",
        "line": 3,
        "description": "The upstream base URL is server-side (entidades-client.ts:3); the client attaches no caller credential and the search is made with the server's own network access."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "A valid session cookie for any role, including `cliente`, is sufficient; no explicit permission is required."
      },
      {
        "kind": "environmental_dependency",
        "description": "The upstream person-search service must be reachable from the server for records to be returned."
      }
    ],
    "execution": {
      "attacker_perspective": "An authenticated low-privilege customer who has selected an event and wants to harvest personal data of arbitrary people.",
      "payloads": [
        "{\"documento\":\"12345678\"}",
        "{\"nombre\":\"a\"}"
      ],
      "instructions": [
        "Log in as any role (e.g. the `cliente` role) and obtain a valid session cookie.",
        "Send POST /api/entidades/persona with a JSON body containing an arbitrary documento or nombre.",
        "Observe the returned ListInfoPersona records containing names, birth date, email, phone and address."
      ],
      "observed_result": "Source analysis establishes that the request passes the auth-only middleware and reaches the upstream person-search service through the server-side client, whose declared response type (PersonaResultDTO) is returned verbatim to the caller; no user identity is forwarded to the upstream."
    },
    "remediation": {
      "strategy": "Register /api/entidades with an explicit permission (e.g. a new entidades:search or reuse of solicitudes:upload) in PROTECTED, and require it in the searchPerson/searchEmpresa controllers; additionally consider rate limiting and reducing the returned PII to what the caller needs.",
      "code_changes": [
        {
          "file_name": "src/middleware.ts",
          "fixed_code": "  { path: \"/api/entidades\", permission: \"entidades:search\" },"
        },
        {
          "file_name": "src/controllers/entidades.controller.ts",
          "fixed_code": "async searchPerson(request: Request): Promise<NextResponse> {\n    const session = await getSession();\n    if (!session) return error(API_ERROR_CODES.UNAUTHORIZED, \"No autorizado\", 401);\n    if (!hasPermission(session, \"entidades:search\")) return error(API_ERROR_CODES.FORBIDDEN, \"Prohibido\", 403);\n    const body = (await request.json()) as { documento?: string; nombre?: string };\n    ..."
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "The endpoint is reachable by any authenticated role and accepts unrestricted DNI/name queries with no rate limit, so exploitation is trivial for an existing low-privilege account."
      },
      "impact": {
        "score": "high",
        "reason": "Successful queries return full natural-person PII (document, names, birth date, email, phone, address), enabling mass personal-data harvesting through the server's upstream access."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "All controls and the PII response shape are visible in repository source; the only external dependency is the upstream service, which the code calls server-side."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "errors/log/unauth-unbounded-errorlog-insert",
    "title": "Unauthenticated, unbounded writes to the ErrorLog table",
    "description": "POST /api/errors/log is not covered by any PROTECTED prefix or public list, so the middleware falls through to NextResponse.next() (middleware.ts:72) and the route is anonymous. The handler accepts message/stack/digest/url/metadata, treats the session as optional (route.ts:21), and inserts a row with no per-field length cap, deduplication, rate limit or retention policy (error-logger.ts:16); the ErrorLog model stores unbounded String/Json columns (schema.prisma:398-410). An anonymous client can therefore grow the table without limit, and nginx only weakly bounds a single request at 50M (nginx.conf:17) without bounding cardinality.",
    "root_cause": "/api/errors is absent from PROTECTED and the public lists, the route never rejects a missing session, and neither the route nor errorLogger.log enforces size, dedup, rate or retention limits.",
    "intended_behavior": "An append-only error store that can be reached without authentication must at least bound per-record size and total cardinality/retention, or be restricted to authenticated clients; the current design provides neither.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 72,
        "scope": "middleware fall-through",
        "description": "/api/errors matches no PROTECTED entry or public list, so the request proceeds unauthenticated."
      },
      {
        "kind": "propagation",
        "file": "src/app/api/errors/log/route.ts",
        "line": 21,
        "scope": "optional session",
        "description": "getSession().catch(() => null) is used only to attach an optional userId; the handler does not require a session."
      },
      {
        "kind": "sink",
        "file": "src/lib/server/error-logger.ts",
        "line": 16,
        "scope": "prisma.errorLog.create",
        "description": "Caller-controlled strings/JSON are persisted with no length, dedup, rate or retention enforcement."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 12,
        "description": "PROTECTED contains no /api/errors entry, and PUBLIC_API_ROUTES/PUBLIC_API_PREFIXES do not list it either."
      },
      {
        "file": "src/app/api/errors/log/route.ts",
        "line": 17,
        "description": "The only input check is that message is truthy; stack, digest, url and metadata are unbounded."
      },
      {
        "file": "prisma/schema.prisma",
        "line": 398,
        "description": "ErrorLog fields message/stack/digest/url are unbounded String and metadata is unbounded Json."
      },
      {
        "file": "docker/nginx.conf",
        "line": 17,
        "description": "client_max_body_size 50M limits a single request body only; it does not bound the number of records or retention."
      }
    ],
    "conditions": [
      {
        "kind": "network_routing",
        "description": "The route must be internet-reachable (Next.js App Router handler served by nginx), which the deployment exposes."
      },
      {
        "kind": "data_state",
        "description": "Sustained impact requires repeated requests to accumulate rows; a single request already creates attacker-controlled rows."
      }
    ],
    "execution": {
      "attacker_perspective": "An anonymous internet client with no account.",
      "payloads": [
        "{\"message\":\"<large repeated string>\",\"metadata\":{\"k\":\"<arbitrary>\"}}"
      ],
      "instructions": [
        "Send POST /api/errors/log with an arbitrary message (and optional stack/url/metadata) without a session cookie.",
        "Repeat the request to accumulate rows."
      ],
      "observed_result": "Source analysis establishes that the request passes the middleware fall-through, no session is required, and a row is inserted for every call; the table has no retention or cardinality bound."
    },
    "remediation": {
      "strategy": "Either require authentication for /api/errors/log or, if it must remain open for client error capture, enforce a max body/message length, drop server-authoritative or oversized fields, apply per-IP rate limiting and a retention/rollover policy on error_log.",
      "code_changes": [
        {
          "file_name": "src/app/api/errors/log/route.ts",
          "fixed_code": "if (typeof body.message !== \"string\" || body.message.length > 2000) {\n    return error(API_ERROR_CODES.VALIDATION, \"message invalido\", 400);\n  }\n  const stack = typeof body.stack === \"string\" ? body.stack.slice(0, 8000) : undefined;\n  // plus per-IP rate limiting and scheduled error_log retention"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "The endpoint is anonymous and unconditional; any client can trigger inserts at will."
      },
      "impact": {
        "score": "medium",
        "reason": "The demonstrated effect is unbounded storage growth and data pollution in error_log (and of any dashboard reading it), with potential availability/storage exhaustion; nginx caps an individual body but not the number of rows."
      },
      "overall_severity": "medium"
    },
    "confidence": {
      "score": "high",
      "reason": "The absence of an auth gate and of any bound is fully visible in middleware.ts, the route, the logger and the Prisma model."
    }
  },
  {
    "verdict": "needs_validation",
    "fingerprint": "facturacion.actualizar.unvalidated-body-mass-assignment",
    "title": "PATCH /api/facturacion/actualizar spreads an unvalidated body into the Prisma update",
    "description": "The actualizar controller destructures only id out of the parsed JSON and forwards the remaining object, cast to a narrow type at compile time but never validated at runtime, through the application service into prisma.facturacion.update({ where: { id }, data }). Extra keys such as estado, solicitudId, montoTotal, moneda, modoPago or flgActivo therefore reach the ORM and are written. Exploitation still requires a principal that can pass the /api/facturacion facturacion:view gate, which no static non-admin role holds.",
    "claimed_root_cause": "auth pattern: the controller performs an unchecked `as` cast of request.json() (no Zod/schema parse), the service signature types data as `{ tipo?: string }` but forwards it unchanged, and the repository passes the whole object to prisma.facturacion.update.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/facturacion/[...slug]/route.ts",
        "line": 15,
        "scope": "PATCH actualizar dispatch",
        "description": "Slug router maps PATCH actualizar to facturacionController.actualizar."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/facturacion.controller.ts",
        "line": 53,
        "scope": "actualizar",
        "description": "const { id, ...data } = (await request.json()) as { id: string; tipo?: string } - a compile-time cast with no runtime validation."
      },
      {
        "kind": "propagation",
        "file": "src/application/facturacion/facturacion-service.ts",
        "line": 22,
        "scope": "actualizar",
        "description": "data is typed { tipo?: string } but passed through to this.repo.actualizar(id, data, createdBy) unchanged."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/facturacion-repository.ts",
        "line": 123,
        "scope": "actualizar",
        "description": "prisma.facturacion.update({ where: { id }, data }) writes every attacker-supplied key that matches a Facturacion column."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 21,
        "description": "Only facturacion:view gates /api/facturacion, so the reachable caller set equals holders of that permission."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 209,
        "description": "facturacion:view is statically granted only to admin, leaving reachability of a non-admin principal unproven."
      },
      {
        "file": "prisma/schema.prisma",
        "line": 412,
        "description": "Facturacion update input admits estado, solicitudId, montoTotal, moneda, modoPago and flgActivo, the columns the unchecked object can overwrite."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "Whether any deployed or custom-created non-admin role holds facturacion:view is a database/deployment fact; no static non-admin role does.",
      "A concrete overwrite of a victim invoice requires a target Facturacion row id in the deployed database (data-state fact)."
    ],
    "validation_plan": {
      "local": "Authenticate as a facturacion:view holder and send PATCH /api/facturacion/actualizar with body {\"id\":\"<invoiceId>\",\"estado\":\"pagado\",\"montoTotal\":1} ; then read the row back and confirm the unintended columns changed.",
      "deployment": "Confirm whether a non-admin role with facturacion:view is provisioned, since admin already holds admin:full and is not a privilege-escalation target."
    }
  },
  {
    "verdict": "needs_validation",
    "fingerprint": "facturacion.agregar-cuota.actualizar.eliminar.eliminar-cuota.unbound-by-id",
    "title": "Billing mutations addressed only by record id lack owner/event binding",
    "description": "POST /api/facturacion/agregar-cuota, PATCH /api/facturacion/actualizar, DELETE /api/facturacion/eliminar and POST /api/facturacion/eliminar-cuota authenticate the caller with getSession() but then read/write the target Facturacion or FacturacionCuota strictly by the caller-supplied id, with no predicate binding the record to the caller's solicitud/empresa/event. The middleware requires only the facturacion:view permission for the whole /api/facturacion prefix, and the read path listar() does narrow by eventoId while the mutation paths do not. If any non-admin principal holds facturacion:view in the deployed role set, that principal can add or delete installments and soft-delete any invoice.",
    "claimed_root_cause": "Repository mutation methods key only on the supplied id (findFirst/create/update/delete by facturacionId or id) with no owner or event predicate, while the sibling listar() read applies an eventoId predicate; controllers forward only the body/query id plus session.email.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/facturacion/[...slug]/route.ts",
        "line": 10,
        "scope": "POST agregar-cuota dispatch",
        "description": "Slug router maps agregar-cuota, pagar-cuota and eliminar-cuota (POST) plus actualizar/eliminar to the facturacion controller."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/facturacion.controller.ts",
        "line": 37,
        "scope": "agregarCuota",
        "description": "getSession() then service.agregarCuota(facturacionId, ...) using the raw request body id with no ownership or event lookup."
      },
      {
        "kind": "propagation",
        "file": "src/infrastructure/persistence/facturacion-repository.ts",
        "line": 93,
        "scope": "agregarCuota",
        "description": "prisma.facturacionCuota.create({ data: { facturacionId, numero, monto, fechaVencimiento } }) inserts an installment into any invoice id supplied by the caller."
      },
      {
        "kind": "propagation",
        "file": "src/infrastructure/persistence/facturacion-repository.ts",
        "line": 123,
        "scope": "actualizar",
        "description": "prisma.facturacion.update({ where: { id }, data }) updates any invoice by id."
      },
      {
        "kind": "propagation",
        "file": "src/infrastructure/persistence/facturacion-repository.ts",
        "line": 132,
        "scope": "eliminar",
        "description": "prisma.facturacion.update({ where: { id }, data: { flgActivo: false } }) soft-deletes any invoice by id."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/facturacion-repository.ts",
        "line": 141,
        "scope": "eliminarCuota",
        "description": "prisma.facturacionCuota.delete({ where: { id: cuotaId } }) deletes any installment by id after a findUnique by the same id."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 21,
        "description": "Only facturacion:view gates the entire /api/facturacion prefix; there is no per-record or per-owner scope."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 209,
        "description": "ROLES_PERMISSIONS grants facturacion:view to no static non-admin role: the admin list (lines 183-208) includes it while logistica, legal, comunicacion and cliente do not."
      },
      {
        "file": "src/infrastructure/persistence/facturacion-repository.ts",
        "line": 10,
        "description": "listar() demonstrates the intended event-scoping pattern (eventoId predicate) that the mutation methods omit."
      },
      {
        "file": "prisma/schema.prisma",
        "line": 412,
        "description": "Facturacion has solicitudId, tipo, estado, montoTotal, moneda, modoPago and flgActivo but no caller-owner column, so scope must derive from the parent solicitud/event."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "Whether any deployed or custom-created non-admin role holds facturacion:view is a database/deployment fact; src/lib/shared/constants.ts grants it statically only to admin, so a reachable non-admin principal is not established from source.",
      "Cross-owner effect requires a victim Facturacion/FacturacionCuota row belonging to another solicitud/event to exist in the deployed database (data-state fact)."
    ],
    "validation_plan": {
      "local": "With a running app and seeded DB, issue a non-admin session whose role includes facturacion:view, then call POST /api/facturacion/agregar-cuota (and PATCH /actualizar, DELETE /eliminar, POST /eliminar-cuota) with the id of an invoice created by a different user and confirm the mutation succeeds.",
      "deployment": "Inspect the deployed role/permission tables to determine whether any non-admin role is granted facturacion:view and whether such a role can be created/assigned through roles:manage."
    }
  },
  {
    "verdict": "needs_validation",
    "fingerprint": "facturacion.niubizz.confirmar.unverified-payment",
    "title": "Niubizz payment confirmation ignores the gateway authorization result before marking a cuota paid",
    "description": "confirmarPago awaits niubizzClient.autorizar(...) but never inspects the returned value, then unconditionally calls facturacionRepo.pagarCuota, which can move the installment, parent invoice and solicitud to PAGADO. It also builds the authorization key from cuota.respuesta_api, a field that detalle() never returns, and passes a fresh String(Date.now()) purchase number, so the authorization is not demonstrably bound to the invoice. Whether a declined authorization is distinguishable from a success depends on the external proxy's HTTP-status versus body semantics, which are not source-visible.",
    "claimed_root_cause": "NiubizzApplicationService.confirmarPago discards the autorizar() result and proceeds to pagarCuota(); niubizz-client postJson() throws only on a non-2xx response and does not examine a success/failure field; the controller calls the flow without getSession().",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/facturacion/niubizz/confirmar/route.ts",
        "line": 3,
        "scope": "POST /api/facturacion/niubizz/confirmar",
        "description": "Route directly exports niubizzController.confirmarPago; access is governed only by the middleware /api/facturacion prefix gate."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/niubizz.controller.ts",
        "line": 33,
        "scope": "confirmarPago",
        "description": "Reads facturacionId and transactionToken from the body with no getSession() call and forwards them to the service."
      },
      {
        "kind": "propagation",
        "file": "src/application/facturacion/niubizz-service.ts",
        "line": 44,
        "scope": "confirmarPago",
        "description": "await niubizzClient.autorizar({ key: cuotaPendiente.respuesta_api ?? \"\", amount, transactionToken, purchaseNumber: String(Date.now()) }) - the result is not assigned or inspected."
      },
      {
        "kind": "propagation",
        "file": "src/application/facturacion/niubizz-service.ts",
        "line": 52,
        "scope": "confirmarPago",
        "description": "this.facturacionRepo.pagarCuota(cuotaPendiente.id, \"niubizz\", null) runs unconditionally and marks the installment paid."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/facturacion-repository.ts",
        "line": 113,
        "scope": "pagarCuota cascade",
        "description": "The repository can cascade facturacion and solicitud to PAGADO once the last installment is marked."
      }
    ],
    "evidence": [
      {
        "file": "src/infrastructure/external/niubizz-client.ts",
        "line": 29,
        "description": "Only `if (!res.ok) throw` is checked; a 200 response with a failure body is returned to callers as-is and never inspected."
      },
      {
        "file": "src/infrastructure/persistence/facturacion-repository.ts",
        "line": 82,
        "description": "detalle() maps only id/numero/monto/fechaVencimiento/estado/comprobante, so respuesta_api is always undefined and the authorization key is the empty string."
      },
      {
        "file": "src/domain/ports/facturacion-repository.ts",
        "line": 12,
        "description": "The port's cuota shape omits respuesta_api, confirming the service reads a field that cannot be populated."
      },
      {
        "file": "src/middleware.ts",
        "line": 21,
        "description": "facturacion:view gates the prefix; no static non-admin role holds it (constants.ts:209)."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "Provider-side behavior is not source-visible: whether Niubizz/IIMP proxy returns a declined authorization as a non-2xx (which would throw and prevent pagarCuota) or as a 2xx body (which would be ignored and allow pagarCuota) cannot be established from the repository.",
      "Whether IIMP_PROXY_*/NIUBIZZ_* credentials are configured and the proxy is reachable in the deployment is a deployment fact.",
      "Whether any deployed non-admin role holds facturacion:view is a database/deployment fact."
    ],
    "validation_plan": {
      "local": "In an OS-enforced sandbox with the app running locally under NODE_ENV not production and no external network, seed one Facturacion with a pending cuota and POST /api/facturacion/niubizz/confirmar with a synthetic transactionToken; confirm that the local mock authorization result is discarded and the cuota is marked PAGADO, showing pagarCuota does not depend on the authorization result.",
      "deployment": "The owner provides the documented or previously captured behavior of the IIMP proxy niubiz.php get_authorization branch for a declined transaction (HTTP status and body shape), so it can be determined whether a decline arrives as a non-2xx status (which throws before pagarCuota) or as a 2xx body the code ignores; no live transaction is run."
    }
  },
  {
    "verdict": "needs_validation",
    "fingerprint": "facturacion.niubizz.sesion.missing-owner-event-binding",
    "title": "Niubizz session creation reads any invoice's data by id with no owner/event binding",
    "description": "crearSesion checks only getSession() and then loads Facturacion.detalle(facturacionId), a findUnique keyed solely by id with no owner or event predicate. It reads the solicitante PII (nombre, apellidos, telefono) and returns a gateway sessionToken, amount and merchantId to the caller. A non-admin holder of facturacion:view could initiate a payment session for any invoice and learn the invoice's solicitante data and amount.",
    "claimed_root_cause": "detalle() is findUnique({ where: { id } }) with no owner/event predicate, and crearSesion forwards the caller-supplied facturacionId straight into it before querying PII and calling the gateway.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/facturacion/niubizz/sesion/route.ts",
        "line": 3,
        "scope": "POST /api/facturacion/niubizz/sesion",
        "description": "Route exports niubizzController.crearSesion; /api/facturacion is gated by facturacion:view in middleware."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/niubizz.controller.ts",
        "line": 15,
        "scope": "crearSesion",
        "description": "getSession() then reads facturacionId from the body and calls niubizzService.crearSesion(facturacionId)."
      },
      {
        "kind": "propagation",
        "file": "src/application/facturacion/niubizz-service.ts",
        "line": 10,
        "scope": "crearSesion",
        "description": "facturacionRepo.detalle(facturacionId) loads the invoice and its cuotas for any id."
      },
      {
        "kind": "sink",
        "file": "src/application/facturacion/niubizz-service.ts",
        "line": 13,
        "scope": "crearSesion PII read",
        "description": "prisma.userRole.findFirst({ where: { email: fact.correoSolicitante } }) fetches the solicitante's nombre/apellidos/telefono and returns session data to the caller."
      }
    ],
    "evidence": [
      {
        "file": "src/infrastructure/persistence/facturacion-repository.ts",
        "line": 61,
        "description": "findUnique({ where: { id } }) with no owner/event predicate, unlike listar() which filters by eventoId."
      },
      {
        "file": "src/middleware.ts",
        "line": 21,
        "description": "facturacion:view is the only gate for the /api/facturacion prefix."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 209,
        "description": "No static non-admin role holds facturacion:view."
      },
      {
        "file": "src/controllers/niubizz.controller.ts",
        "line": 20,
        "description": "Response returns sessionToken, amount and merchantId for the requested invoice."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "Whether any deployed or custom-created non-admin role holds facturacion:view is a database/deployment fact.",
      "Reading a victim's PII/session requires a third-party Facturacion row and its solicitante user record to exist in the deployed database (data-state fact)."
    ],
    "validation_plan": {
      "local": "Authenticate as a facturacion:view holder and call POST /api/facturacion/niubizz/sesion with another user's facturacionId, confirming the session token/amount and solicitante data are returned.",
      "deployment": "Verify whether any non-admin role holds facturacion:view and whether invoice ownership is expected to be enforced per solicitante."
    }
  },
  {
    "verdict": "needs_validation",
    "fingerprint": "facturacion.pagar-cuota.missing-owner-event-binding",
    "title": "POST /api/facturacion/pagar-cuota settles any installment by id with no owner/event binding",
    "description": "pagarCuota authenticates only via getSession() and then updates the FacturacionCuota addressed by the request-body cuotaId to PAGADO with no predicate tying the installment to the caller's solicitud/empresa/event. When no pending installments remain it cascades the parent Facturacion and the Solicitud to PAGADO. Because the /api/facturacion prefix requires facturacion:view, a non-admin holder of that permission could settle another party's invoice.",
    "claimed_root_cause": "pagarCuota performs findUnique/update keyed solely by the supplied cuotaId, and its cascade updates facturacion/solicitud by the parent id, with no owner or eventoId predicate; the read path listar() applies an eventoId filter that pagarCuota lacks.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/facturacion/[...slug]/route.ts",
        "line": 11,
        "scope": "POST pagar-cuota dispatch",
        "description": "Slug router maps pagar-cuota to facturacionController.pagarCuota."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/facturacion.controller.ts",
        "line": 44,
        "scope": "pagarCuota",
        "description": "getSession() then service.pagarCuota(cuotaId, session.email, comprobante) using the raw body cuotaId."
      },
      {
        "kind": "propagation",
        "file": "src/infrastructure/persistence/facturacion-repository.ts",
        "line": 102,
        "scope": "pagarCuota",
        "description": "prisma.facturacionCuota.update({ where: { id: cuotaId }, data: { estado: PAGADO } }) settles any installment by id."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/facturacion-repository.ts",
        "line": 113,
        "scope": "pagarCuota cascade",
        "description": "When no pending installments remain, prisma.facturacion.update and prisma.solicitud.update set both to PAGADO for that parent record."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 21,
        "description": "facturacion:view is the only gate for the /api/facturacion prefix."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 209,
        "description": "No static non-admin role holds facturacion:view."
      },
      {
        "file": "src/infrastructure/persistence/facturacion-repository.ts",
        "line": 10,
        "description": "listar() shows the intended evento/solicitud scoping that pagarCuota omits."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "Whether any deployed or custom-created non-admin role holds facturacion:view is a database/deployment fact.",
      "Reaching the PAGADO cascade requires a victim invoice with all installments settleable in the deployed database (data-state fact)."
    ],
    "validation_plan": {
      "local": "As a facturacion:view holder, call POST /api/facturacion/pagar-cuota with a cuotaId belonging to a different solicitud and confirm the cuota and parent facturacion/solicitud change state.",
      "deployment": "Identify deployed non-admin roles holding facturacion:view and whether payment confirmation is expected to be scoped per solicitante."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "gess.listar.unauth-stand-inventory-disclosure",
    "title": "Unauthenticated GESS stand-inventory disclosure across all events",
    "description": "GET /api/gess/listar is not covered by any middleware gate (middleware.ts:72); gessController.listar performs no session check and accepts any eventoId or bloqueId (gess.controller.ts:8-31). The repository runs prisma.gessStand.findMany({ where: { eventoId } }) with no `select`, returning every column including empresa, email, userId and rawData (gess-repository.ts:14-20; schema.prisma:181-212). Because the public /api/eventos/listar?presala=1 endpoint exposes real eventoIds, an anonymous client can enumerate stand inventory (exhibitor company and email, internal userId, raw upstream data) for any event.",
    "root_cause": "The /api/gess route family is absent from the middleware authorization list, the listar controller never authenticates the caller or binds the event to the session, and the repository returns the full row without a field projection.",
    "intended_behavior": "A stand-inventory read should be authenticated/authorized by the middleware gate and bound to the caller's event scope, and should expose only the fields the caller is entitled to see.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 72,
        "scope": "middleware fall-through",
        "description": "/api/gess/listar is neither protected nor public, so it is reachable without a session."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/gess.controller.ts",
        "line": 10,
        "scope": "gessController.listar",
        "description": "eventoId and bloqueId are read from the query and used with no session or permission check."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/gess-repository.ts",
        "line": 14,
        "scope": "GessPrismaRepository.findAllPaginated",
        "description": "findMany without a select returns all GessStand columns, including empresa, email, userId and rawData."
      }
    ],
    "evidence": [
      {
        "file": "src/infrastructure/persistence/gess-repository.ts",
        "line": 32,
        "description": "findByBloque likewise returns a full row for any bloqueId with no authorization."
      },
      {
        "file": "prisma/schema.prisma",
        "line": 194,
        "description": "GessStand exposes email, userId and rawData columns that the unauthenticated response includes."
      },
      {
        "file": "src/middleware.ts",
        "line": 44,
        "description": "The presala exception makes /api/eventos/listar?presala=1 public, providing real eventoIds for enumeration."
      },
      {
        "file": "src/app/api/gess/[...slug]/route.ts",
        "line": 6,
        "description": "GET maps the `listar` slug to gessController.listar with no guard."
      }
    ],
    "conditions": [
      {
        "kind": "data_state",
        "description": "Stand rows must exist for the queried eventoId for data to be returned."
      }
    ],
    "execution": {
      "attacker_perspective": "An anonymous internet client.",
      "payloads": [
        "GET /api/gess/listar?eventoId=<id>&per_page=100",
        "GET /api/gess/listar?bloqueId=<bloqueId>"
      ],
      "instructions": [
        "Obtain a real eventoId from the public GET /api/eventos/listar?presala=1 endpoint.",
        "Request GET /api/gess/listar?eventoId=<id> without a session cookie.",
        "Observe the returned stand rows including empresa, email, userId and rawData."
      ],
      "observed_result": "Source analysis establishes that the GET passes the middleware fall-through, the controller performs no auth check, and the repository returns the full GessStand row set for the caller-supplied eventoId."
    },
    "remediation": {
      "strategy": "Add /api/gess to PROTECTED with a suitable permission, require getSession and bind eventoId to the session in listar, and project only the fields the caller needs (excluding email/userId/rawData).",
      "code_changes": [
        {
          "file_name": "src/middleware.ts",
          "fixed_code": "  { path: \"/api/gess\", permission: \"stands:plano\" },"
        },
        {
          "file_name": "src/infrastructure/persistence/gess-repository.ts",
          "fixed_code": "prisma.gessStand.findMany({\n        where: where as never,\n        select: { id: true, standApiId: true, standCode: true, tipoStand: true, estado: true, bloqueId: true },\n        orderBy: { standCode: \"asc\" },\n        skip: (params.page - 1) * params.perPage,\n        take: params.perPage,\n      })"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "Anonymous, unrestricted by id, and eventoIds are available from a public endpoint."
      },
      "impact": {
        "score": "high",
        "reason": "Discloses exhibitor company names, contact email, internal userId and raw upstream data for arbitrary events."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "The missing middleware entry, absent controller check, and full-row select are all directly visible in source."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "gess/actualizar/unauth-arbitrary-id-update",
    "title": "Unauthenticated update of any GessStand by id",
    "description": "PATCH /api/gess/actualizar is dispatched through the [...slug] router (gess/[...slug]/route.ts:12-14) but /api/gess appears in neither PROTECTED nor the public lists, so the middleware allows it anonymously (middleware.ts:72). gessController.actualizar performs no session check (gess.controller.ts:33-37); it parses updateGessStandSchema (which accepts any id plus estado/documentos/imagenes/bloqueId, gess.validator.ts:3-9) and calls services.gess.actualizarStand, which calls repo.update(id, data) -> prisma.gessStand.update({ where: { id } }) with no owner or event predicate (gess-repository.ts:56-59). An anonymous caller can therefore modify any stand's state, documents or images across all events.",
    "root_cause": "The /api/gess route family is absent from the middleware authorization list and the controller/repository apply no authentication, permission, or event/owner predicate, so a client-supplied id is written directly.",
    "intended_behavior": "Updating a GESS stand must require the caller to be authenticated and authorized for that event, and the update must be bound to the caller's scope; arbitrary id writes by anonymous clients must be denied.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 72,
        "scope": "middleware fall-through",
        "description": "/api/gess matches no PROTECTED prefix and no public list, so the request proceeds unauthenticated."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/gess.controller.ts",
        "line": 33,
        "scope": "gessController.actualizar",
        "description": "The handler parses the body with updateGessStandSchema and calls services.gess.actualizarStand with no getSession/authorization check."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/gess-repository.ts",
        "line": 57,
        "scope": "GessPrismaRepository.update",
        "description": "prisma.gessStand.update({ where: { id }, data }) writes the attacker-selected record with no owner/evento predicate."
      }
    ],
    "evidence": [
      {
        "file": "src/validators/gess.validator.ts",
        "line": 3,
        "description": "updateGessStandSchema accepts id, bloqueId, documentos, imagenes and estado with no event/ownership field."
      },
      {
        "file": "src/application/gess/gess-service.ts",
        "line": 57,
        "description": "actualizarStand forwards the parsed object (including attacker-chosen estado/documentos/imagenes) to the repository."
      },
      {
        "file": "src/app/api/gess/[...slug]/route.ts",
        "line": 12,
        "description": "PATCH maps the `actualizar` slug to gessController.actualizar without any wrapper guard."
      },
      {
        "file": "prisma/schema.prisma",
        "line": 181,
        "description": "GessStand rows hold estado, empresa, documentos, imagenes and rawData that the unauthenticated update can rewrite."
      }
    ],
    "conditions": [
      {
        "kind": "data_state",
        "description": "The target GessStand id must exist for the update to persist (Prisma throws otherwise); ids are discoverable via the unauthenticated listar endpoint."
      }
    ],
    "execution": {
      "attacker_perspective": "An anonymous internet client.",
      "payloads": [
        "{\"id\":\"<gessStand.id>\",\"estado\":\"reservado\",\"documentos\":[\"https://attacker.example/x\"]}"
      ],
      "instructions": [
        "Obtain a GessStand id from the unauthenticated GET /api/gess/listar?eventoId=... response.",
        "Send PATCH /api/gess/actualizar with that id and arbitrary estado/documentos/imagenes, without any session cookie."
      ],
      "observed_result": "Source analysis establishes that the middleware lets the PATCH through and the repository updates the row selected solely by the attacker-supplied id."
    },
    "remediation": {
      "strategy": "Add /api/gess to the middleware PROTECTED list with an appropriate permission (and event binding), and require getSession plus an event/owner-scoped predicate in actualizarStand/the repository.",
      "code_changes": [
        {
          "file_name": "src/middleware.ts",
          "fixed_code": "  { path: \"/api/gess\", permission: \"stands:manage\" },"
        },
        {
          "file_name": "src/controllers/gess.controller.ts",
          "fixed_code": "async actualizar(request: Request): Promise<NextResponse> {\n    const session = await getSession();\n    if (!session) return error(API_ERROR_CODES.UNAUTHORIZED, \"No autorizado\", 401);\n    const body = updateGessStandSchema.parse(await request.json());\n    return success(await services.gess.actualizarStand(body.id, body, session.eventoId));\n  }"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "The route is anonymous and accepts any id; no secret or account is needed."
      },
      "impact": {
        "score": "high",
        "reason": "Integrity of the stand inventory, including reservation-relevant estado, empresa and documentos, can be rewritten for arbitrary records and events."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "The full chain (no middleware match, no controller check, id-only repository write) is directly visible in source."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "gess/mockup/unauth-stand-state-reset",
    "title": "Unauthenticated mockup generation resets GessStand state to disponible and clears empresa",
    "description": "POST /api/gess/mockup is not covered by any middleware gate (middleware.ts:72) and gessController.mockup performs no session or permission check (gess.controller.ts:50-59). After resolving the event's planos, gessService.mockup upserts each bloque's stand forcing estado: \"disponible\" and empresa: null (gess-service.ts:124-145, specifically :132-133). An anonymous caller who supplies an existing eventoId (with tipoEvento/codigoEvento) can therefore reset live stand state and detach the company attached to each stand.",
    "root_cause": "The /api/gess family has no middleware protection and the mockup handler has no authorization check, while the generator unconditionally overwrites estado/empresa on existing rows matched by (eventoId, bloqueId).",
    "intended_behavior": "A demo data generator is a privileged administrative action; it must require authorization and must not downgrade existing production stand state.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 72,
        "scope": "middleware fall-through",
        "description": "/api/gess/mockup is neither protected nor public, so it is reachable anonymously."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/gess.controller.ts",
        "line": 50,
        "scope": "gessController.mockup",
        "description": "The handler validates that eventoId/tipoEvento/codigoEvento are present but never authenticates or authorizes the caller."
      },
      {
        "kind": "sink",
        "file": "src/application/gess/gess-service.ts",
        "line": 132,
        "scope": "GessApplicationService.mockup upsert",
        "description": "Each matched stand is updated with estado: \"disponible\" and empresa: null (line 133), overwriting live values."
      }
    ],
    "evidence": [
      {
        "file": "src/application/gess/gess-service.ts",
        "line": 124,
        "description": "The loop calls findByStandApiId(eventoId, b.bloqueId) and then repo.update(exists.id, data) for existing stands."
      },
      {
        "file": "src/application/gess/gess-service.ts",
        "line": 133,
        "description": "data.empresa is hard-coded to null, clearing any company currently linked to the stand."
      },
      {
        "file": "src/app/api/gess/[...slug]/route.ts",
        "line": 10,
        "description": "POST maps the `mockup` slug to gessController.mockup with no guard."
      }
    ],
    "conditions": [
      {
        "kind": "data_state",
        "description": "Real effect requires existing GessStand rows for the targeted event/bloques; the planos must resolve for tipoEvento/codigoEvento."
      }
    ],
    "execution": {
      "attacker_perspective": "An anonymous internet client.",
      "payloads": [
        "{\"eventoId\":\"<existing-evento-id>\",\"tipoEvento\":1,\"codigoEvento\":1}"
      ],
      "instructions": [
        "Send POST /api/gess/mockup with an existing eventoId and valid tipoEvento/codigoEvento, without a session cookie.",
        "Re-read the affected stands (e.g. via GET /api/gess/listar) to observe estado reset to disponible and empresa cleared."
      ],
      "observed_result": "Source analysis establishes that the handler runs unauthenticated and the service's update path forces estado: \"disponible\" and empresa: null on every matched stand."
    },
    "remediation": {
      "strategy": "Require authentication plus an administrative permission for mockup (and the whole /api/gess family), and gate demo generation behind a non-production environment check so it cannot overwrite live state.",
      "code_changes": [
        {
          "file_name": "src/middleware.ts",
          "fixed_code": "  { path: \"/api/gess\", permission: \"stands:manage\" },"
        },
        {
          "file_name": "src/controllers/gess.controller.ts",
          "fixed_code": "async mockup(request: Request): Promise<NextResponse> {\n    const session = await getSession();\n    if (!session) return error(API_ERROR_CODES.UNAUTHORIZED, \"No autorizado\", 401);\n    if (!hasPermission(session, \"stands:manage\")) return error(API_ERROR_CODES.FORBIDDEN, \"Prohibido\", 403);\n    ..."
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "The endpoint is anonymous and deterministic; only an existing eventoId and event codes are needed."
      },
      "impact": {
        "score": "high",
        "reason": "Live stand state and company linkage are silently reset, corrupting reservation/occupancy data for the targeted event."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "The missing gate and the forced reset values are both explicit in source."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "gess/sync/unauth-import-authority-expansion",
    "title": "Unauthenticated GESS import allows arbitrary stand creation/overwrite for any event",
    "description": "POST /api/gess/sync is not covered by the middleware (middleware.ts:72) and gessController.sync performs no authentication or authorization (gess.controller.ts:39-48). It forwards the caller-supplied eventoId (any string) and seleccionadas array to gessService.sync, which upserts GessStand rows with caller-controlled uid, tipoStand, estado, empresa, pabellon and rawData (gess-service.ts:61-98). An anonymous caller can inject or overwrite stand inventory for any event, expanding their authority beyond any authorized data set.",
    "root_cause": "The /api/gess route is missing from both PROTECTED and the public lists, the sync controller never calls getSession, and the service accepts arbitrary eventoId plus caller-supplied rows without validating authority or event binding.",
    "intended_behavior": "A bulk import must enforce authentication, an import permission, and event binding, and should not let the caller choose which event's data is written.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 72,
        "scope": "middleware fall-through",
        "description": "/api/gess/sync is allowed anonymously because no prefix matches it."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/gess.controller.ts",
        "line": 39,
        "scope": "gessController.sync",
        "description": "The handler takes body.eventoId/seleccionadas directly and calls services.gess.sync with no session or permission check."
      },
      {
        "kind": "sink",
        "file": "src/application/gess/gess-service.ts",
        "line": 88,
        "scope": "GessApplicationService.sync upsert",
        "description": "For each caller row the service updates or creates a GessStand with attacker-controlled fields under the chosen eventoId."
      }
    ],
    "evidence": [
      {
        "file": "src/application/gess/gess-service.ts",
        "line": 62,
        "description": "rows come from body.seleccionadas when non-empty, i.e. entirely attacker-controlled."
      },
      {
        "file": "src/application/gess/gess-service.ts",
        "line": 79,
        "description": "The persisted data object maps caller fields directly into standApiId/standCode/tipoStand/estado/empresa/rawData."
      },
      {
        "file": "src/infrastructure/persistence/gess-repository.ts",
        "line": 51,
        "description": "create/update go straight to prisma.gessStand with no authorization predicate."
      },
      {
        "file": "src/app/api/gess/[...slug]/route.ts",
        "line": 9,
        "description": "POST maps the `sync` slug to gessController.sync with no guard wrapper."
      }
    ],
    "conditions": [
      {
        "kind": "data_state",
        "description": "Creating rows always succeeds for a valid eventoId relation; overwriting requires matching (eventoId, standApiId) rows."
      }
    ],
    "execution": {
      "attacker_perspective": "An anonymous internet client.",
      "payloads": [
        "{\"eventoId\":\"<any-evento-id>\",\"tipoEvento\":0,\"codigoEvento\":0,\"seleccionadas\":[{\"uid\":\"ATTACKER-1\",\"tipo\":\"PREFERENCIAL\",\"status\":\"reservado\",\"company\":\"Attacker SA\"}]}"
      ],
      "instructions": [
        "Send POST /api/gess/sync with an arbitrary eventoId and a seleccionadas array, without a session cookie.",
        "Query GET /api/gess/listar?eventoId=... to confirm the injected stand rows were persisted."
      ],
      "observed_result": "Source analysis establishes that the controller forwards the body unauthenticated and the service creates/updates GessStand rows from the caller-supplied array for the caller-chosen event."
    },
    "remediation": {
      "strategy": "Protect the /api/gess family with authentication and an imports permission, derive eventoId from the session instead of the body where possible, and validate the imported rows against a schema.",
      "code_changes": [
        {
          "file_name": "src/middleware.ts",
          "fixed_code": "  { path: \"/api/gess\", permission: \"stands:manage\" },"
        },
        {
          "file_name": "src/controllers/gess.controller.ts",
          "fixed_code": "async sync(request: Request): Promise<NextResponse> {\n    const session = await getSession();\n    if (!session) return error(API_ERROR_CODES.UNAUTHORIZED, \"No autorizado\", 401);\n    const body = syncGessSchema.parse(await request.json());\n    return success(await services.gess.sync(body.eventoId, body.tipoEvento, body.codigoEvento, body.seleccionadas));\n  }"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "Anonymous and unconditional; a single crafted request is enough."
      },
      "impact": {
        "score": "high",
        "reason": "Arbitrary creation/overwrite of stand inventory for any event undermines data integrity and any downstream reservation logic that trusts gess_stand."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "The missing gate and the caller-controlled upsert fields are explicit in the route, controller and service."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "github.workflows.deploy.yml:ec2-git-token-persisted-in-git-config",
    "title": "GitHub token embedded in git remote URL and persisted in host git config",
    "description": "The deploy script sets the EC2 working-copy remote to https://x-access-token:${{ secrets.EC2_GIT_TOKEN }}@github.com/iimp-projects/contratos-stands.git. Git stores remote URLs verbatim in .git/config, so the token is persisted in cleartext on the production host after every deploy and is also visible in process arguments and any config/command logging.",
    "root_cause": "A long-lived credential (EC2_GIT_TOKEN) is placed directly in the git remote URL instead of being supplied by an ephemeral credential helper or a scoped deploy key.",
    "intended_behavior": "Use a least-privilege, short-lived credential (deploy key or credential helper) and never persist tokens in the repository remote URL.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": ".github/workflows/deploy.yml",
        "line": 124,
        "scope": "deploy job remote script",
        "description": "git remote set-url origin https://x-access-token:${{ secrets.EC2_GIT_TOKEN }}@github.com/... — token placed in the remote URL."
      },
      {
        "kind": "sink",
        "file": "docker-compose.prod.yml",
        "line": 52,
        "scope": "EC2 host working copy",
        "description": "The host repository (source bind-mounted into the app) retains .git/config with the credentialed remote URL."
      }
    ],
    "evidence": [
      {
        "file": ".github/workflows/deploy.yml",
        "line": 124,
        "description": "Token interpolated into the origin remote URL, which git writes verbatim to .git/config on the host."
      },
      {
        "file": "scripts/deploy.sh",
        "line": 26,
        "description": "The manual deploy script performs a plain git pull without a token, showing the credentialed remote is an automatable leftover rather than a requirement."
      }
    ],
    "conditions": [
      {
        "kind": "system_configuration",
        "description": "The deploy step runs on push to main whenever EC2_SECRETS are configured, writing the token-bearing remote on the production host."
      }
    ],
    "execution": {
      "attacker_perspective": "Local or container-level attacker on the production EC2 host who can read the working-copy .git/config (the app container root user also bind-mounts this tree).",
      "payloads": [
        "grep -R x-access-token /var/www/contratos-stands/.git/config",
        "git -C /var/www/contratos-stands remote -v"
      ],
      "instructions": [
        "Static reproduction (no target execution performed): read .github/workflows/deploy.yml:124 and confirm the token is interpolated into the remote URL.",
        "Deployment-time reproduction: after a deploy, read the host repository's .git/config and observe the token embedded in the origin URL."
      ],
      "observed_result": "Source shows the remote URL contains the token; git's documented behavior persists that URL to .git/config, leaving the credential on disk until rotated."
    },
    "remediation": {
      "strategy": "Authenticate git with a short-lived, least-privilege GitHub App token via a credential helper (or an SSH deploy key) and never embed credentials in the remote URL; scope and rotate EC2_GIT_TOKEN.",
      "code_changes": [
        {
          "file_name": ".github/workflows/deploy.yml",
          "fixed_code": "            git remote set-url origin git@github.com:iimp-projects/contratos-stands.git\n            git -c credential.helper='!f() { echo \"username=x-access-token\"; echo \"password=$GIT_TOKEN\"; }; f' -c credential.helper='cache --timeout=0' pull origin main"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "medium",
        "reason": "Any process, backup or root-equivalent actor on the EC2 host (including the root app container that bind-mounts the repo) can read the token from .git/config."
      },
      "impact": {
        "score": "high",
        "reason": "A GitHub token exposed on the deploy host grants repository read/write and, via workflows, further supply-chain access."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "Line 124 explicitly places the token in the remote URL; git's persistence of remote URLs in .git/config is deterministic behavior, not a deployment assumption."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "github.workflows.deploy.yml:ssh-action-mutable-tag-with-prod-secrets",
    "title": "Deploy workflow invokes a mutable third-party action tag while handing it production EC2 credentials",
    "description": "The CD deploy job in .github/workflows/deploy.yml invokes appleboy/ssh-action@v1.2.2, a movable version tag rather than a pinned commit, and supplies it with the production host, SSH private key and git token. Whatever code that tag resolves to at run time executes on the GitHub runner with those secrets, and the job runs automatically on every push to main without a protected environment.",
    "root_cause": "The third-party action is referenced by a mutable semver tag (v1.2.2) instead of an immutable full commit SHA, so its content can be changed after review and is not integrity-bound.",
    "intended_behavior": "Third-party actions that receive production credentials must be pinned to a full commit SHA (or vendored) and the deploy job must be gated by a protected GitHub Environment.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": ".github/workflows/deploy.yml",
        "line": 113,
        "scope": "deploy job — Deploy via SSH step",
        "description": "uses: appleboy/ssh-action@v1.2.2 — action resolved from a mutable tag."
      },
      {
        "kind": "propagation",
        "file": ".github/workflows/deploy.yml",
        "line": 117,
        "scope": "with: key/host/username",
        "description": "Production secrets EC2_SSH_KEY, EC2_HOST, EC2_USERNAME and EC2_GIT_TOKEN are passed into the mutable action."
      },
      {
        "kind": "sink",
        "file": ".github/workflows/deploy.yml",
        "line": 124,
        "scope": "remote script executed by the action",
        "description": "The action runs a remote script that consumes EC2_GIT_TOKEN, so moving the tag yields execution with the production deploy credential."
      }
    ],
    "evidence": [
      {
        "file": ".github/workflows/deploy.yml",
        "line": 113,
        "description": "uses: appleboy/ssh-action@v1.2.2 — tag reference, not a 40-char commit SHA."
      },
      {
        "file": ".github/workflows/deploy.yml",
        "line": 117,
        "description": "key: ${{ secrets.EC2_SSH_KEY }} — production SSH private key exposed to the action."
      },
      {
        "file": ".github/workflows/deploy.yml",
        "line": 96,
        "description": "Deploy job condition runs on push to refs/heads/main with no environment protection or approval gate."
      }
    ],
    "conditions": [
      {
        "kind": "system_configuration",
        "description": "Deploy job runs automatically on push to main; no GitHub Environment protection is declared in source, so a moved upstream tag is consumed without human review."
      }
    ],
    "execution": {
      "attacker_perspective": "Supply-chain attacker able to move the v1.2.2 tag in (or compromise) the upstream appleboy/ssh-action repository.",
      "payloads": [
        "Re-point the appleboy/ssh-action v1.2.2 tag to an attacker-controlled commit that exfiltrates the with: inputs."
      ],
      "instructions": [
        "Static reproduction (no target execution performed): inspect .github/workflows/deploy.yml, confirm line 113 resolves the action from tag v1.2.2 rather than a SHA, and confirm lines 115-124 pass EC2_SSH_KEY and EC2_GIT_TOKEN into that action.",
        "Deployment-time replay: move the upstream tag to attacker code and push any commit to main; the deploy job then runs the moved code with the production secrets."
      ],
      "observed_result": "Source establishes the mutable-tag reference and the secret inputs; no integrity binding to a reviewed commit exists, so the credentials are exposed to whatever code the tag points to at run time."
    },
    "remediation": {
      "strategy": "Pin all third-party actions to full commit SHAs (e.g. appleboy/ssh-action@<40-hex-sha>) and require reviewer approval via a protected GitHub Environment for the deploy job.",
      "code_changes": [
        {
          "file_name": ".github/workflows/deploy.yml",
          "fixed_code": "        uses: appleboy/ssh-action@<immutable-40-char-commit-sha> # v1.2.2"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "low",
        "reason": "Requires the upstream maintainer (or a compromise of the upstream repo) to move the tag; no evidence of that has occurred."
      },
      "impact": {
        "score": "high",
        "reason": "Successful substitution yields the EC2 SSH private key and a long-lived GitHub token, enabling arbitrary production code execution and repository access."
      },
      "overall_severity": "medium"
    },
    "confidence": {
      "score": "high",
      "reason": "The tag reference and the secret inputs are explicit in the workflow source; only the tag-move precondition is external."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "gitignore:.env.prod-not-ignored",
    "title": ".gitignore omits .env.prod / .env.production despite production requiring them",
    "description": ".gitignore excludes .env, .env.local, the *.local variants and *.pem/*.key, but has no rule matching .env.prod or .env.production. Deployment docs and docker-compose.prod.yml require a populated .env.prod containing DB_PASSWORD and JWT_SECRET in the working tree, so these production secrets can be accidentally committed.",
    "root_cause": "The env-file ignore patterns enumerate `.env`, `.env.local`, `.env.development.local`, `.env.test.local`, `.env.production.local` and `.env*.local`, none of which match the `.env.prod` name actually used by the project.",
    "intended_behavior": "Every secret-bearing environment file used by the project, including .env.prod and .env.production, must be excluded from version control.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": ".gitignore",
        "line": 34,
        "scope": "variables de entorno block",
        "description": "Ignore list starts at .env but never includes .env.prod/.env.production."
      },
      {
        "kind": "propagation",
        "file": "docker-compose.prod.yml",
        "line": 41,
        "scope": "app service env_file",
        "description": "Production service requires .env.prod in the working tree."
      },
      {
        "kind": "sink",
        "file": ".env.prod.example",
        "line": 19,
        "scope": "secret template",
        "description": "Template documents DB_PASSWORD and JWT_SECRET that the real .env.prod will hold."
      }
    ],
    "evidence": [
      {
        "file": ".gitignore",
        "line": 39,
        "description": ".env*.local matches only files ending in .local; .env.prod and .env.production do not match any earlier line."
      },
      {
        "file": "docs/01-despliegue/despliegue.md",
        "line": 81,
        "description": "Deployment instructions require a populated .env.prod with real DB_PASSWORD/JWT_SECRET in the working tree."
      },
      {
        "file": ".env.prod.example",
        "line": 20,
        "description": "DATABASE_URL/DB_PASSWORD and JWT_SECRET are the secret contents at risk."
      }
    ],
    "conditions": [
      {
        "kind": "system_configuration",
        "description": "Materializes only once an operator creates a real .env.prod from the example; the current tracked tree contains only .env.example and .env.prod.example."
      }
    ],
    "execution": {
      "attacker_perspective": "Opportunistic or careless committer whose local .env.prod is staged by a broad git add and pushed to the remote.",
      "payloads": [
        "git add .env.prod && git commit -m 'config' && git push"
      ],
      "instructions": [
        "Static reproduction (no target execution performed): apply the .gitignore patterns to the string '.env.prod' — no pattern matches.",
        "Verify with `git check-ignore -v .env.prod .env.production` in the repository root; both names are unignored."
      ],
      "observed_result": "No .gitignore rule matches .env.prod/.env.production, so a populated production secrets file would be tracked and pushed if staged."
    },
    "remediation": {
      "strategy": "Add explicit ignore rules for all production env files before any real .env.prod exists.",
      "code_changes": [
        {
          "file_name": ".gitignore",
          "fixed_code": "# variables de entorno\n.env\n.env.local\n.env.*.local\n.env.prod\n.env.production\n.env.prod.*\n!.env.prod.example"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "medium",
        "reason": "Requires a real .env.prod to be created and staged, which the documented workflow makes routine; no secret is currently committed."
      },
      "impact": {
        "score": "high",
        "reason": "A committed .env.prod exposes the production database password and JWT signing secret, enabling full data access and session forgery."
      },
      "overall_severity": "medium"
    },
    "confidence": {
      "score": "high",
      "reason": "The ignore patterns and the required .env.prod name are both explicit in source and do not intersect."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "kbservicios.events.unauth-internal-proxy",
    "title": "Unauthenticated access to the internal KBServicios events proxy",
    "description": "POST /api/kbservicios/events forwards a caller-supplied code to the legacy KBServicios /rest/events endpoint, but the route is absent from both the middleware PROTECTED list and the public allowlists, and the controller performs no session check. The middleware therefore falls through with NextResponse.next(), and any anonymous caller can drive the internal integration proxy and consume its response.",
    "root_cause": "src/middleware.ts authorizes only paths that prefix-match PROTECTED (lines 50-51) and lets every other path through (line 72); /api/kbservicios matches none of PROTECTED or the PUBLIC lists, and kbServiciosController.listarEventos (src/controllers/kbservicios.controller.ts:9) never calls getSession, so the internal proxy has no authentication.",
    "intended_behavior": "A server-side integration proxy should require an authenticated/authorized principal or the M2M API key before it relays a caller-controlled request to the legacy service.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 50,
        "scope": "PROTECTED prefix loop",
        "description": "No PROTECTED entry prefix-matches /api/kbservicios and it is not in PUBLIC_API_PREFIXES or PUBLIC_API_ROUTES, so the loop completes and line 72 calls NextResponse.next() for an anonymous request."
      },
      {
        "kind": "propagation",
        "file": "src/app/api/kbservicios/[...slug]/route.ts",
        "line": 6,
        "scope": "POST events dispatch",
        "description": "createRouter dispatches the anonymous POST to kbServiciosController.listarEventos."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/kbservicios.controller.ts",
        "line": 9,
        "scope": "listarEventos",
        "description": "Reads body.code (default 14) and calls services.kbServicios.listarEventos with no getSession or permission check."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/external/kbservicios-client.ts",
        "line": 8,
        "scope": "fetchApi",
        "description": "Issues a server-side POST to ${KBSERVICIOS_URL}/rest/events with the caller-supplied code and cache no-store; the request carries no caller credential."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 12,
        "description": "PROTECTED list contains /api/auspicios, /api/entidades and others, but no /api/kbservicios entry."
      },
      {
        "file": "src/middleware.ts",
        "line": 72,
        "description": "Unmatched paths fall through to NextResponse.next()."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 30,
        "description": "PUBLIC_API_ROUTES does not include /api/kbservicios."
      },
      {
        "file": "src/controllers/kbservicios.controller.ts",
        "line": 9,
        "description": "listarEventos has no session or authorization check."
      },
      {
        "file": "src/infrastructure/external/kbservicios-client.ts",
        "line": 44,
        "description": "listarEventos posts to /rest/events with the caller code."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "anonymous: no token cookie is required."
      },
      {
        "kind": "environmental_dependency",
        "description": "KBSERVICIOS_URL must be configured for the client to be constructed (it throws at import otherwise), which holds in the declared production environment."
      }
    ],
    "execution": {
      "attacker_perspective": "Unauthenticated HTTP client.",
      "payloads": [
        "POST /api/kbservicios/events body {\"code\":14} with no Cookie header"
      ],
      "instructions": [
        "Issue the POST without a token cookie.",
        "Observe that middleware does not redirect to /auth/login because no PROTECTED prefix matches.",
        "Observe the handler returns the KBServicios event list through services.kbServicios.listarEventos."
      ],
      "observed_result": "Not executed (no OS-enforced sandbox on the audit host); the source trace deterministically shows the anonymous request is gated by neither middleware nor the controller and reaches the fetchApi call to the legacy events endpoint."
    },
    "remediation": {
      "strategy": "Add an explicit PROTECTED entry for the /api/kbservicios prefix requiring an appropriate permission (for example events:manage) plus a getSession guard in kbServiciosController, or gate it with the M2M x-api-key like the other integration routes; make middleware deny by default for /api paths."
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "the endpoint is reachable anonymously with a single request and needs no credentials."
      },
      "impact": {
        "score": "low",
        "reason": "the proxy returns legacy event metadata and the client applies no operator credential, so the demonstrated effect is anonymous use of an internal integration surface."
      },
      "overall_severity": "low"
    },
    "confidence": {
      "score": "high",
      "reason": "the middleware fall-through, the missing controller check, and the absence from the public lists are all directly visible in source."
    }
  },
  {
    "verdict": "needs_validation",
    "fingerprint": "planos.exportar-ts.unescaped-codegen",
    "title": "TypeScript code generation interpolates stored layout fields without escaping",
    "description": "exportarTypeScript builds TypeScript source by raw template interpolation of plano.codigo, nombre, descripcion, tipos[].codigo/label/nombre/color, bloques[].bloqueId/tipoCodigo/tipologia and furniture fields. None of the interpolated values are escaped, and the validators bound length only, not characters, so a double quote or newline stored in a name/code/color can break out of the emitted string/identifier literals. The endpoint is gated by requireAdmin (laboratorio:manage or admin:full) and the generated string is returned as JSON, never evaluated in-repo; impact depends on a downstream consumer compiling the artifact.",
    "claimed_root_cause": "The codegen template literals interpolate database-backed values directly and tsCodegenUtils provides no string/identifier escaping; toConstName only uppercases and replaces hyphens, leaving other characters untouched.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/planos/[...slug]/route.ts",
        "line": 11,
        "scope": "GET exportar-ts dispatch",
        "description": "Slug router maps GET exportar-ts to planosController.exportarTs."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/planos.controller.ts",
        "line": 94,
        "scope": "exportarTs",
        "description": "requireAdmin() then reads id from the query and calls services.planos.exportarTypeScript(id)."
      },
      {
        "kind": "propagation",
        "file": "src/application/planos/planos-service.ts",
        "line": 157,
        "scope": "tipos codegen",
        "description": "`${t.codigo}: { w: ${t.w}, d: ${t.d}, h: ${t.h}, color: \"${t.color}\" },` interpolates codigo and color raw into emitted TS."
      },
      {
        "kind": "propagation",
        "file": "src/application/planos/planos-service.ts",
        "line": 184,
        "scope": "items codegen",
        "description": "`{ id: \"${b.bloqueId}\", ... tipologia: \"${b.tipologia ?? ...}\", ... }` interpolates bloqueId/tipologia raw."
      },
      {
        "kind": "sink",
        "file": "src/application/planos/planos-service.ts",
        "line": 226,
        "scope": "registry codegen",
        "description": "nombre: \"${plano.nombre}\", descripcion: \"${plano.descripcion ?? \"\"}\" interpolate stored free text raw into emitted TS."
      }
    ],
    "evidence": [
      {
        "file": "src/lib/shared/utils/ts-codegen.ts",
        "line": 8,
        "description": "toUnionType joins `\"${c}\"` with no escaping of quotes/backslashes/newlines."
      },
      {
        "file": "src/validators/planos.validator.ts",
        "line": 22,
        "description": "planoLayoutSchema constrains codigo/label/nombre/color by length only (z.string().min(1).max(...)), so quote and newline characters pass."
      },
      {
        "file": "src/application/planos/planos-service.ts",
        "line": 154,
        "description": "toConstName(plano.codigo) is likewise only uppercased with hyphens replaced, so an identifier can contain other injected characters."
      },
      {
        "file": "src/controllers/planos.controller.ts",
        "line": 11,
        "description": "requireAdmin restricts the endpoint to laboratorio:manage/admin:full holders."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "The generated string is returned as JSON and is not compiled or evaluated anywhere in this repository; whether and how a downstream tree compiles the copied artifact is a deployment/process fact.",
      "Whether any deployed or custom-created non-admin role holds laboratorio:manage (or admin:full) is a database/deployment fact; the statically privileged caller is already trusted for lab management."
    ],
    "validation_plan": {
      "local": "As a laboratorio:manage holder, store a plano whose nombre/descripcion/color contains a double quote and newline, call GET /api/planos/exportar-ts, then run the emitted string through tsc to show it is syntactically broken or contains injected statements.",
      "deployment": "Establish whether the generated artifact is copied into and compiled by the frontend build, and whether a non-admin principal can hold laboratorio:manage."
    }
  },
  {
    "verdict": "needs_validation",
    "fingerprint": "planos.importar.unvalidated-layout",
    "title": "Plano import persists layout values bypassing the create/layout schema constraints",
    "description": "The importar path is gated by requireAdmin (laboratorio:manage or admin:full) but its Zod schema types tipos, bloques and furniture as z.unknown() and codigo as only z.string().min(1), then imports through repo.importar which calls guardarLayout directly, bypassing both planoLayoutSchema and the ^[a-z0-9-]+$ codigo regex enforced by crear(). Unvalidated values therefore reach the persisted layout and any subsequently emitted code. Whether this crosses a trust boundary depends on whether a non-admin holds laboratorio:manage and whether the persisted/emitted layout is consumed by a downstream build.",
    "claimed_root_cause": "planoImportarSchema uses z.unknown() for tipos/bloques/furniture and omits the create/layout regexes, while PlanoPrismaRepository.importar persists via guardarLayout without re-validating the layout or codigo.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/planos/[...slug]/route.ts",
        "line": 21,
        "scope": "POST importar dispatch",
        "description": "Slug router maps POST importar to planosController.importar; /api/planos is gated by laboratorio:view in middleware and requireAdmin inside the controller."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/planos.controller.ts",
        "line": 104,
        "scope": "importar",
        "description": "requireAdmin() then planoImportarSchema.parse(await request.json()) - the parsed body is cast `as never` and forwarded."
      },
      {
        "kind": "propagation",
        "file": "src/validators/planos.validator.ts",
        "line": 54,
        "scope": "planoImportarSchema",
        "description": "tipos: z.array(z.unknown()), bloques: z.array(z.unknown()), furniture: z.array(z.unknown()).default([]), and codigo: z.string().min(1) without the create regex."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/plano-repository.ts",
        "line": 388,
        "scope": "importar",
        "description": "this.guardarLayout(plano.id, { tipos: data.tipos, bloques: ..., furniture: ... }) persists the unchecked layout directly."
      }
    ],
    "evidence": [
      {
        "file": "src/application/planos/planos-service.ts",
        "line": 31,
        "description": "crear() enforces /^[a-z0-9-]+$/ which importar never applies."
      },
      {
        "file": "src/application/planos/planos-service.ts",
        "line": 146,
        "description": "importar() checks only that codigo/nombre exist and tipos/bloques are arrays, then delegates to the repository."
      },
      {
        "file": "src/controllers/planos.controller.ts",
        "line": 11,
        "description": "requireAdmin requires laboratorio:manage or admin:full, a privileged caller set."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 183,
        "description": "laboratorio:manage is listed only in the admin role's permissions."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "Whether any deployed or custom-created non-admin role holds laboratorio:manage (or admin:full) is a database/deployment fact; requireAdmin otherwise limits the caller to a fully trusted principal.",
      "Whether the persisted layout is later fed into a build/compile step (documented workflow reportedly copies artifacts into an external frontend tree) is not source-visible in this repository."
    ],
    "validation_plan": {
      "local": "Authenticate as a laboratorio:manage holder and POST /api/planos/importar with a codigo containing disallowed characters and tipos/bloques of arbitrary shape, then reload the plano and confirm the invalid values persisted.",
      "deployment": "Determine whether any non-admin role holds laboratorio:manage and whether an automated process consumes imported/exported plano artifacts in a compiled tree."
    }
  },
  {
    "verdict": "needs_validation",
    "fingerprint": "reniec/dni/unauth-paid-pii-lookup",
    "title": "Unauthenticated RENIEC DNI lookup under the server-side operator token",
    "description": "GET /api/reniec/dni validates only the 8-digit format and then calls the external provider api.apis.net.pe with a server-side Bearer token. The route is absent from both PROTECTED and the public allowlists, so middleware lets anonymous callers through, and the controller never establishes a session. The claimed consequence is that an unauthenticated caller can drive operator-credentialed PII lookups and read returned identity data; that consequence depends on provider and deployment facts that source does not establish.",
    "claimed_root_cause": "src/middleware.ts:50-72 lets /api/reniec through because no PROTECTED prefix matches; consultas.controller.ts:23-35 checks only an 8-digit pattern and calls consultasClient.consultarDni, which in consultas-client.ts:9-10 attaches Authorization: Bearer ${SUNAT_API_TOKEN} to the provider request.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 50,
        "scope": "PROTECTED prefix loop",
        "description": "No PROTECTED entry prefix-matches /api/reniec and it is not in the PUBLIC lists, so the request passes through to the route."
      },
      {
        "kind": "propagation",
        "file": "src/app/api/reniec/[...slug]/route.ts",
        "line": 6,
        "scope": "GET dni dispatch",
        "description": "createRouter dispatches the anonymous GET to reniecController.consultarDni."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/consultas.controller.ts",
        "line": 26,
        "scope": "consultarDni format check",
        "description": "Only /^\\d{8}$/ is enforced; no getSession or permission check precedes the provider call."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/external/consultas-client.ts",
        "line": 9,
        "scope": "fetchAPI provider request",
        "description": "Issues GET to the fixed DNI URL with Authorization: Bearer ${SUNAT_API_TOKEN}; with the token unset the function throws before calling the provider."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 72,
        "description": "Unmatched paths fall through to NextResponse.next()."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 30,
        "description": "PUBLIC_API_ROUTES does not include /api/reniec."
      },
      {
        "file": "src/controllers/consultas.controller.ts",
        "line": 26,
        "description": "Only an 8-digit pattern is validated; no session check."
      },
      {
        "file": "src/infrastructure/external/consultas-client.ts",
        "line": 3,
        "description": "The operator token is read from SUNAT_API_TOKEN."
      },
      {
        "file": "src/infrastructure/external/consultas-client.ts",
        "line": 24,
        "description": "consultarDni targets the fixed api.apis.net.pe DNI URL."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "Whether SUNAT_API_TOKEN is provisioned in the deployed task is not source-visible; consultas-client.ts:8 throws before any provider call when it is unset.",
      "The provider response (whether api.apis.net.pe returns full identity data) and the provider-side authorization are external facts not present in the repository."
    ],
    "validation_plan": {
      "local": "In an OS-enforced sandbox with an allowlisted empty environment, run the /api/reniec/dni handler against a loopback stub for api.apis.net.pe using a dummy token and assert that an anonymous request with no Cookie reaches the handler and issues the Bearer request; use no external network.",
      "deployment": "An owner confirms whether SUNAT_API_TOKEN is configured and observes that an unauthenticated GET /api/reniec/dni?numero=<dummy 8 digits> from outside returns provider data (or 502 when the token is unset) with no token cookie present."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "reservas/crear/unauth-stand-block-and-solicitud",
    "title": "Unauthenticated stand reservation blocks inventory and creates solicitudes",
    "description": "POST /api/reservas/crear calls getSession() but discards a null result, so an anonymous caller proceeds. The service marks each requested stand as en_evaluacion, records the caller e-mail/user, then creates a solicitud with three review records, alert rows, and confirmation e-mails. The route is not protected by middleware, so an unauthenticated attacker can block stand inventory and inject bogus solicitudes into the review workflow.",
    "root_cause": "reserva.controller.ts:10 obtains the session but never returns 401 when it is null; it forwards session?.email and session?.sub (undefined) into services.reservas.crear, and reserva-service.ts:27-43 and 52-59 apply the state transition and create the solicitud regardless. No PROTECTED prefix matches /api/reservas in middleware.ts.",
    "intended_behavior": "Creating a reservation mutates shared stand state and starts an approval workflow; it must require an authenticated owner (and the write:reservas permission) before any state change.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 50,
        "scope": "PROTECTED prefix loop",
        "description": "No PROTECTED entry matches /api/reservas and it is not public, so the anonymous request falls through at line 72."
      },
      {
        "kind": "propagation",
        "file": "src/app/api/reservas/[...slug]/route.ts",
        "line": 6,
        "scope": "POST crear dispatch",
        "description": "createRouter dispatches the anonymous POST to reservaController.crear."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/reserva.controller.ts",
        "line": 10,
        "scope": "crear",
        "description": "getSession() is called but its null result is not rejected; the request continues with session?.email and session?.sub undefined."
      },
      {
        "kind": "sink",
        "file": "src/application/reservas/reserva-service.ts",
        "line": 37,
        "scope": "stand state transition",
        "description": "gessRepo.update sets the stand estado to en_evaluacion, stores the caller e-mail/user and optional documentos, and then lines 52-59 create the solicitud and three review records."
      }
    ],
    "evidence": [
      {
        "file": "src/controllers/reserva.controller.ts",
        "line": 10,
        "description": "Session is read but never rejected when null."
      },
      {
        "file": "src/controllers/reserva.controller.ts",
        "line": 15,
        "description": "session?.email and session?.sub default to undefined for anonymous callers."
      },
      {
        "file": "src/application/reservas/reserva-service.ts",
        "line": 38,
        "description": "ESTADOS_STAND.EN_EVALUACION is assigned to the stand."
      },
      {
        "file": "src/application/reservas/reserva-service.ts",
        "line": 52,
        "description": "crearSolicitud plus three crearRevisionInicial calls create the workflow record."
      },
      {
        "file": "src/application/reservas/reserva-service.ts",
        "line": 106,
        "description": "Admin notification e-mail is sent from the unauthenticated flow."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 212,
        "description": "cliente is the role expected to create reservations (write:reservas); that permission is never enforced."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "anonymous: no token cookie is required."
      },
      {
        "kind": "data_state",
        "description": "the attacker must submit stand identifiers that exist and are not already blocked; enumerable GessStand ids make this practical."
      }
    ],
    "execution": {
      "attacker_perspective": "Unauthenticated HTTP client.",
      "payloads": [
        "POST /api/reservas/crear body {\"standIds\":[\"<existing-stand-id>\"],\"datos\":{\"razonSocial\":\"x\",\"tipoDocumento\":\"DNI\",\"numeroDocumento\":\"1\",\"email\":\"a@b.com\"}} with no Cookie header"
      ],
      "instructions": [
        "Issue the POST with no token cookie.",
        "Observe that getSession() returns null but the controller still calls services.reservas.crear.",
        "Observe that the stand row is updated to en_evaluacion and a solicitud with comunicacion, legal, and logistica revision rows is created."
      ],
      "observed_result": "Not executed (no OS-enforced sandbox on the audit host); the source trace deterministically shows a null session is not rejected and the stand update plus solicitud and revision creation run for the anonymous caller."
    },
    "remediation": {
      "strategy": "Reject null sessions in reservaController.crear with a 401 and enforce write:reservas, then re-check owner/event scope in the service before mutating stands; add /api/reservas to the middleware PROTECTED list and make /api deny by default."
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "the endpoint is reachable anonymously and the reservation ids are ordinary request values."
      },
      "impact": {
        "score": "high",
        "reason": "an anonymous actor can remove stands from availability for every other client and inject bogus solicitudes and e-mails into a shared review workflow."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "the null-session path, the service writes, and the middleware omission are directly visible at the cited lines."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "solicitudes.detalle.missing-owner-binding",
    "title": "GET /api/solicitudes/detalle returns any solicitud by UUID to any solicitudes:view holder",
    "description": "solicitudesController.detalle parses a caller-supplied id and calls services.solicitudes.detalle(id) without any getSession-based ownership check (solicitudes.controller.ts:32-38), and the repository resolves it with prisma.solicitud.findUnique({ where: { id } }) with no owner or event predicate (solicitudes-repository.ts:185-199). The middleware gate for /api/solicitudes only requires solicitudes:view (middleware.ts:28), which the `cliente` role holds (constants.ts:212). Consequently any authenticated customer can read the full detail of any solicitud — including solicitante email, empresa, documents, revision history and facturacion identifiers — simply by supplying its UUID.",
    "root_cause": "The read is keyed solely on the client-supplied primary key with no predicate binding the record to the session subject, and the only authorization is the broad solicitudes:view permission that customers also hold.",
    "intended_behavior": "Reading a single solicitud must be bound to the authenticated subject: a `cliente` may only read a record whose userId equals session.sub (or whose event the reviewer is entitled to), returning NOT_FOUND/FORBIDDEN otherwise.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/solicitudes/[...slug]/route.ts",
        "line": 7,
        "scope": "GET detalle dispatch",
        "description": "Slug router maps GET /api/solicitudes/detalle to solicitudesController.detalle; prefix gated by solicitudes:view."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 35,
        "scope": "detalle",
        "description": "const row = await services.solicitudes.detalle(id) — the caller-supplied id is used directly with no session and no owner comparison (the controller never calls getSession)."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 186,
        "scope": "detalle",
        "description": "prisma.solicitud.findUnique({ where: { id: solicitudId } }) resolves any record by primary key, returning the full mapped row."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 28,
        "description": "Only solicitudes:view gates the /api/solicitudes prefix; there is no per-record ownership scope."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 212,
        "description": "The `cliente` role holds solicitudes:view, so customers reach the endpoint."
      },
      {
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 34,
        "description": "solicitudesDetalleSchema.parse validates only that id is a UUID; nothing binds it to the caller."
      },
      {
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 191,
        "description": "The include returns revisiones, reevaluaciones, docsAdjuntos and facturaciones, i.e. full contract detail for the selected record."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "Any session carrying solicitudes:view (including the customer role) is sufficient."
      },
      {
        "kind": "data_state",
        "description": "The attacker must know or guess the target solicitud UUID; the unscoped listar endpoint makes those identifiers enumerable."
      }
    ],
    "execution": {
      "attacker_perspective": "An authenticated customer who wants to read another company's solicitud detail.",
      "payloads": [
        "GET /api/solicitudes/detalle?id=<victimSolicitudUuid>"
      ],
      "instructions": [
        "Authenticate as any role holding solicitudes:view, e.g. `cliente`.",
        "Obtain a target solicitud UUID (e.g. from the unscoped listar endpoint).",
        "Call GET /api/solicitudes/detalle?id=<uuid> and read the returned record."
      ],
      "observed_result": "Source analysis establishes that the request passes middleware (solicitudes:view) and reaches findUnique by id with no owner predicate, so the record of any owner is returned to any solicitudes:view holder."
    },
    "remediation": {
      "strategy": "Require a session in detalle, load the record, and deny when the caller is a customer whose session.sub differs from the record's userId; alternatively scope the repository lookup by userId/eventoId for non-privileged callers.",
      "code_changes": [
        {
          "file_name": "src/controllers/solicitudes.controller.ts",
          "fixed_code": "async detalle(request: Request): Promise<NextResponse> {\n    const session = await getSession();\n    if (!session) return error(API_ERROR_CODES.UNAUTHORIZED, \"No autorizado\", 401);\n    const url = new URL(request.url);\n    const { id } = solicitudesDetalleSchema.parse({ id: url.searchParams.get(\"id\") ?? \"\" });\n    const row = await services.solicitudes.detalle(id);\n    if (!row) return error(API_ERROR_CODES.NOT_FOUND, \"Solicitud no encontrada\", 404);\n    const esInterno = session.permissions.some((p) => p.startsWith(\"solicitudes:review:\") || p === \"admin:full\");\n    if (!esInterno && row.userId && row.userId !== session.sub) {\n      return error(API_ERROR_CODES.NOT_FOUND, \"Solicitud no encontrada\", 404);\n    }\n    return success(row);\n  }"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "Any authenticated customer can call it and target identifiers are enumerable via the unscoped listar endpoint."
      },
      "impact": {
        "score": "high",
        "reason": "Returns another customer's full solicitud detail, including PII, documents and billing identifiers."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "Controller and repository code are fully visible and contain no owner check on the read path."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "solicitudes.historial.missing-session-and-owner",
    "title": "GET /api/solicitudes/historial exposes any solicitud's reviewer history and justifications to any solicitudes:view holder",
    "description": "Authentication for /api/solicitudes is supplied by middleware, which requires a valid JWT with solicitudes:view (middleware.ts:28); the candidate's 'missing session' wording is therefore inaccurate for the end-to-end path. The real defect is the missing per-record owner binding: solicitudesController.historial reads the id from the query and calls repo.obtenerHistorial(id) with no session, permission or ownership check (solicitudes.controller.ts:159-163), and obtenerHistorial queries prisma.revision.findMany / prisma.revisionHistorial.findMany using only { where: { solicitudId } } (solicitudes-repository.ts:374-384). Any `cliente` (constants.ts:212) can thus read the review history of any solicitud, including reviewer emails (createdBy/updatedBy) and previous justifications (comentarioAnterior).",
    "root_cause": "The history repository methods filter exclusively by the caller-supplied solicitudId and the controller neither authenticates the subject against the record nor restricts the endpoint to reviewers; the only control is the blanket solicitudes:view permission shared by customers.",
    "intended_behavior": "A history endpoint that returns reviewer identities and justifications must require an entitled reviewer (area permission or admin) and/or bind the record to the caller; customers must only see history for their own solicitudes.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/solicitudes/[...slug]/route.ts",
        "line": 8,
        "scope": "GET historial dispatch",
        "description": "Slug router maps GET /api/solicitudes/historial to solicitudesController.historial."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 163,
        "scope": "historial",
        "description": "const { revisiones, historial } = await services.solicitudes.repo.obtenerHistorial(gessStandId) — the query id is passed straight through; there is no session or ownership check in the handler."
      },
      {
        "kind": "propagation",
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 376,
        "scope": "obtenerHistorial revisiones",
        "description": "prisma.revision.findMany({ where: { solicitudId }, select: { ..., createdBy, updatedBy, comentario } }) returns reviewer identity and comments for any solicitud id."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 381,
        "scope": "obtenerHistorial historial",
        "description": "prisma.revisionHistorial.findMany({ where: { solicitudId } }) returns estadoAnterior/comentarioAnterior/motivo/createdBy for any solicitud id."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 28,
        "description": "The endpoint is authenticated (contrary to the 'missing session' framing) but only by the broad solicitudes:view permission."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 212,
        "description": "The `cliente` role holds solicitudes:view and can therefore reach historial."
      },
      {
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 160,
        "description": "Only the id query parameter is validated; no session, role or owner check is performed."
      },
      {
        "file": "prisma/schema.prisma",
        "line": 331,
        "description": "RevisionHistorial stores createdBy (reviewer) and comentarioAnterior (justification), which are returned to the caller."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "A valid session with solicitudes:view (including the customer role) is required and sufficient; the endpoint is not anonymous."
      },
      {
        "kind": "data_state",
        "description": "A target solicitud with prior reviews/history must exist; solicitud UUIDs are enumerable through the unscoped listar endpoint."
      }
    ],
    "execution": {
      "attacker_perspective": "An authenticated customer who wants to inspect the review trail and reviewer identities of another company's application.",
      "payloads": [
        "GET /api/solicitudes/historial?id=<victimSolicitudUuid>"
      ],
      "instructions": [
        "Authenticate as any role holding solicitudes:view, e.g. `cliente`.",
        "Obtain a target solicitud UUID.",
        "Call GET /api/solicitudes/historial?id=<uuid> and read the returned revision/historial items containing usuario (reviewer email) and justifications."
      ],
      "observed_result": "Source analysis establishes that the middleware admits any solicitudes:view holder and the repository returns all Revision and RevisionHistorial rows for the supplied solicitudId with no owner predicate."
    },
    "remediation": {
      "strategy": "Require a reviewer permission (solicitudes:review:* or admin:full) for historial and, when exposing it to customers, first verify that the solicitud belongs to session.sub; do not rely on the blanket solicitudes:view permission for reviewer-only data.",
      "code_changes": [
        {
          "file_name": "src/controllers/solicitudes.controller.ts",
          "fixed_code": "async historial(request: Request): Promise<NextResponse> {\n    const session = await getSession();\n    if (!session) return error(API_ERROR_CODES.UNAUTHORIZED, \"No autorizado\", 401);\n    const gessStandId = new URL(request.url).searchParams.get(\"id\");\n    if (!gessStandId) return error(API_ERROR_CODES.VALIDATION, \"id requerido\", 400);\n    const detalle = await services.solicitudes.detalle(gessStandId);\n    if (!detalle) return error(API_ERROR_CODES.NOT_FOUND, \"Solicitud no encontrada\", 404);\n    const esRevisor = session.permissions.some((p) => p.startsWith(\"solicitudes:review:\") || p === \"admin:full\");\n    if (!esRevisor && detalle.userId !== session.sub) {\n      return error(API_ERROR_CODES.FORBIDDEN, \"Sin permisos\", 403);\n    }\n    ...\n  }"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "Reachable by any customer and only requires a solicitud UUID, which is enumerable via listar."
      },
      "impact": {
        "score": "medium",
        "reason": "Discloses internal reviewer identities, prior states and justifications for another owner's solicitud; sensitive process metadata rather than credentials."
      },
      "overall_severity": "medium"
    },
    "confidence": {
      "score": "high",
      "reason": "The controller, repository filters and middleware permission are all source-visible; only the reviewer/customer role split is needed and it is in constants."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "solicitudes.listar.unscoped-owner-and-event",
    "title": "GET /api/solicitudes/listar derives owner scope from the query string, exposing every solicitud in an event to any solicitudes:view holder",
    "description": "The listar controller reads userId and eventoId directly from the request query (solicitudes.controller.ts:15-21) and forwards them to the repository, which applies the owner predicate only when params.userId is truthy (solicitudes-repository.ts:152-154) and filters by the attacker-supplied eventoId (solicitudes-repository.ts:146-151). The middleware gate for /api/solicitudes requires only the broad solicitudes:view permission (middleware.ts:28), which the lowest-trust `cliente` role holds (constants.ts:212). Because the owner filter is conditional, a customer can simply omit userId (or point it at another user) and receive every solicitud for the event, including the solicitante email, empresa, document URLs and revision comments returned by mapRow. There is no server-side derivation of the subject from the session and no role distinction between internal reviewers (who may list the whole event) and customers (who must be limited to their own records).",
    "root_cause": "The controller takes the tenant/owner scope from client-controlled query parameters instead of the verified session, and the repository's owner predicate is applied only when userId is truthy rather than denying by default; middleware authorizes the whole prefix with a single blanket permission that customers also hold.",
    "intended_behavior": "Owner/tenant scope must be derived server-side: a `cliente` may only list solicitudes whose userId equals session.sub, regardless of query parameters, while reviewers/admins may be scoped by their event. The client must not be able to widen its own scope by omitting or overriding userId.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/solicitudes/[...slug]/route.ts",
        "line": 6,
        "scope": "GET listar dispatch",
        "description": "The slug router maps GET /api/solicitudes/listar to solicitudesController.listar; the whole prefix is behind middleware.ts:28 (solicitudes:view)."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 21,
        "scope": "listar",
        "description": "const userId = url.searchParams.get(\"userId\") ?? undefined — the owner filter is taken verbatim from the query, with no getSession()/role check."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 15,
        "scope": "listar",
        "description": "eventoId is read from the query and parsed as a UUID, letting the caller choose any event."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 152,
        "scope": "listar",
        "description": "if (params.userId) { where.userId = params.userId } — the owner predicate is absent whenever userId is omitted, so the query returns all rows for the event."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 28,
        "description": "{ path: \"/api/solicitudes\", permission: \"solicitudes:view\" } — a single blanket permission for all read and write actions, held by customers."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 212,
        "description": "ROLES.CLIENTE holds solicitudes:view, so the lowest-trust role passes the middleware gate and can call listar."
      },
      {
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 146,
        "description": "Event filtering is applied from params.eventoId (the query value) rather than from the session's eventoId claim."
      },
      {
        "file": "src/middleware.ts",
        "line": 44,
        "description": "/api/eventos/listar?presala=1 is a public exception, so real eventoId values are obtainable without authentication."
      },
      {
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 120,
        "description": "mapRow returns email, empresa, documentos, docsAdjuntos URLs and revision comments, so the cross-owner list is a data disclosure, not just an id leak."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "Any session whose JWT carries solicitudes:view, including the `cliente` role, is sufficient; no additional permission or role check is performed."
      },
      {
        "kind": "data_state",
        "description": "The target event must contain solicitudes belonging to other owners for the disclosure to include third-party data."
      }
    ],
    "execution": {
      "attacker_perspective": "An authenticated low-privilege customer who wants to enumerate other companies' stand applications for an event.",
      "payloads": [
        "GET /api/solicitudes/listar?eventoId=<eventoUuid>&page=1&per_page=50",
        "GET /api/solicitudes/listar?eventoId=<eventoUuid>&userId=<victimSub>"
      ],
      "instructions": [
        "Log in as the `cliente` role and obtain a valid session cookie.",
        "Obtain an eventoId from the public GET /api/eventos/listar?presala=1 response.",
        "Call GET /api/solicitudes/listar with that eventoId and no userId, and read the returned solicitudes of other customers."
      ],
      "observed_result": "Source analysis establishes that the request passes the solicitudes:view gate (middleware.ts:28) and reaches a Prisma findMany whose where clause contains only the event predicate when userId is omitted (solicitudes-repository.ts:146-154); the response therefore contains all solicitudes of that event, including those created by other users."
    },
    "remediation": {
      "strategy": "Derive the owner scope from the session inside the controller: when the caller's role is `cliente` (or lacks an internal-review permission) force userId = session.sub and ignore the query value; only reviewers/admins whose session eventoId authorizes the event may receive an unscoped event listing. Additionally, make the repository predicate deny-by-default for non-privileged callers instead of conditionally applying it.",
      "code_changes": [
        {
          "file_name": "src/controllers/solicitudes.controller.ts",
          "fixed_code": "async listar(request: Request): Promise<NextResponse> {\n    const session = await getSession();\n    if (!session) return error(API_ERROR_CODES.UNAUTHORIZED, \"No autorizado\", 401);\n    const url = new URL(request.url);\n    const parsed = solicitudesListarSchema.parse({ ... });\n    const esInterno = session.permissions.some((p) => p.startsWith(\"solicitudes:review:\") || p === \"admin:full\");\n    const userId = esInterno ? (url.searchParams.get(\"userId\") ?? undefined) : session.sub;\n    const result = await services.solicitudes.listar({ ..., userId });\n    return success(result);\n  }"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "The endpoint is reachable by any authenticated customer, the owner filter is optional, and the required eventoId is publicly discoverable; a single request enumerates the whole event's solicitudes."
      },
      "impact": {
        "score": "high",
        "reason": "Cross-customer disclosure of contract solicitud data (solicitante email, empresa, document URLs, review comments and state) enables competitive and personal-data harvesting."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "The controller, repository predicate and middleware gate are all fully visible in source; no runtime or deployment fact is required to establish that other-owner rows are returned."
    }
  },
  {
    "verdict": "needs_validation",
    "fingerprint": "solicitudes.modificar.null-owner-bypass",
    "title": "modificar ownership guard short-circuits when Solicitud.userId is null",
    "description": "solicitudesController.modificar guards with `if (detalle.userId && detalle.userId !== session.sub && !session.permissions.includes(\"admin:full\"))` (solicitudes.controller.ts:94). Because the ownership comparison is gated on detalle.userId being truthy, a solicitud whose userId is null bypasses the check for every authenticated caller, who can then reset all of its revisions to pendiente (controller.ts:100-102). Solicitud.userId is nullable (schema.prisma:221) and /api/reservas/crear is absent from PROTECTED and the public lists, so the middleware falls through (middleware.ts:72) and reservaController.crear reads an optional session and passes userSub undefined, leading crearSolicitud to write userId: null (solicitudes-repository.ts:265). Null-owner rows are therefore producible, but whether an exploitable row exists in the deployed database — and whether it has completed revisions needed to pass the controller's all-revisions-non-pending guard (controller.ts:97) — is a data-state fact that cannot be established from source.",
    "claimed_root_cause": "The authorization predicate is written as `detalle.userId && ...` instead of denying by default, so a null owner makes the entire ownership comparison vacuously pass; combined with a nullable Solicitud.userId and an unauthenticated reservation path that writes null owners, the deny-by-default invariant is not enforced.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/solicitudes/[...slug]/route.ts",
        "line": 13,
        "scope": "POST modificar dispatch",
        "description": "Slug router maps POST /api/solicitudes/modificar to solicitudesController.modificar."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 94,
        "scope": "modificar ownership guard",
        "description": "if (detalle.userId && detalle.userId !== session.sub && !admin) — a null/falsy owner short-circuits the guard and admits any caller."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 101,
        "scope": "modificar revision reset",
        "description": "For each revision the controller calls services.solicitudes.revisar(..., estado: PENDIENTE), resetting the workflow of the target solicitud."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/reserva.controller.ts",
        "line": 15,
        "scope": "crear",
        "description": "userEmail/userSub are taken from an optional session (getSession() may return null) and forwarded to reservas.crear."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 265,
        "scope": "crearSolicitud",
        "description": "userId: userId ?? null — solicitudes created through the unauthenticated /api/reservas path persist a null owner."
      }
    ],
    "evidence": [
      {
        "file": "prisma/schema.prisma",
        "line": 221,
        "description": "Solicitud.userId is optional (String? @map(\"user_id\")), so null owners are a valid stored state."
      },
      {
        "file": "src/middleware.ts",
        "line": 72,
        "description": "/api/reservas matches neither PROTECTED nor the public lists, so the middleware returns NextResponse.next() and the route is anonymous."
      },
      {
        "file": "src/middleware.ts",
        "line": 28,
        "description": "modificar itself is reachable by any solicitudes:view holder, including `cliente`."
      },
      {
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 97,
        "description": "modificar refuses while any revision is pendiente, so a null-owner row must have completed its three reviews to be resettable — a data-state precondition."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "Existence of a Solicitud row with userId = NULL in the deployed database (and, per controller.ts:97, one whose revisions are all non-pending) is a data-state fact that cannot be observed from source.",
      "Whether any such null-owner row belongs to a distinct party (as opposed to an anonymous reservation) determines whether the bypass crosses a real ownership boundary; the repository does not record a creator for null-owner reservations."
    ],
    "validation_plan": {
      "local": "With a seeded database, insert a Solicitud with userId NULL and three non-pending revisions, then call POST /api/solicitudes/modificar as an unrelated authenticated user with that id and confirm the 403 guard is skipped and the revisions reset.",
      "deployment": "Query the deployed solicitud table for rows with user_id IS NULL that have completed revisions, and trace whether the public /api/reservas flow is used to create reservations for logged-out clients."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "solicitudes.orden-pago.missing-facturacion-permission",
    "title": "POST /api/solicitudes/orden-pago moves any solicitud into PENDIENTE_PAGO and creates an invoice without the facturacion permission",
    "description": "solicitudesController.ordenPago performs only a session check and calls repo.marcarOrdenPago(solicitudId) with the body id (solicitudes.controller.ts:150-156). marcarOrdenPago sets the solicitud estado to PENDIENTE_PAGO and creates or updates a Facturacion record with a computed montoTotal (solicitudes-repository.ts:349-371). The only gate is the solicitudes:view permission on /api/solicitudes (middleware.ts:28); the facturacion:view permission that guards /api/facturacion (middleware.ts:21) is never required here. Any `cliente` (constants.ts:212) can therefore push any solicitud into the billing pipeline and mint an invoice.",
    "root_cause": "The billing-state transition is exposed under the general solicitudes prefix and the controller checks only for a session; no facturacion permission or ownership binding is applied.",
    "intended_behavior": "Transitioning a solicitud into a billing state and creating a Facturacion record must require an explicit facturacion permission (or admin:full), as the dedicated /api/facturacion endpoints do.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/solicitudes/[...slug]/route.ts",
        "line": 17,
        "scope": "POST orden-pago dispatch",
        "description": "Slug router maps POST /api/solicitudes/orden-pago to solicitudesController.ordenPago."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 151,
        "scope": "ordenPago",
        "description": "Only `const session = await getSession()` and a presence check are performed; the body solicitudId is then forwarded with no permission or ownership check."
      },
      {
        "kind": "propagation",
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 350,
        "scope": "marcarOrdenPago",
        "description": "prisma.solicitud.update sets estado = PENDIENTE_PAGO for the supplied id."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 363,
        "scope": "marcarOrdenPago",
        "description": "prisma.facturacion.create({ data: { solicitudId, tipo: MANUAL, montoTotal, moneda } }) creates the invoice for any target solicitud."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 28,
        "description": "solicitudes:view is the only gate for /api/solicitudes, which serves orden-pago."
      },
      {
        "file": "src/middleware.ts",
        "line": 21,
        "description": "The dedicated billing endpoints require facturacion:view, confirming that billing actions are meant to be permission-restricted."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 212,
        "description": "The `cliente` role holds solicitudes:view and therefore reaches orden-pago."
      },
      {
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 368,
        "description": "The created Facturacion record uses a montoTotal derived from the stand data, so the caller influences financial records."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "Any session with solicitudes:view is sufficient; the customer role qualifies."
      },
      {
        "kind": "system_configuration",
        "description": "The solicitud must be in a state the repository does not otherwise restrict; marcarOrdenPago performs no state guard."
      }
    ],
    "execution": {
      "attacker_perspective": "An authenticated customer who wants to force another solicitud into the billing pipeline or create an invoice.",
      "payloads": [
        "POST /api/solicitudes/orden-pago {\"solicitudId\":\"<victimSolicitudUuid>\"}"
      ],
      "instructions": [
        "Authenticate as any holder of solicitudes:view.",
        "Obtain a target solicitud UUID.",
        "POST /api/solicitudes/orden-pago and observe the estado change to pendiente_pago and the new Facturacion row."
      ],
      "observed_result": "Source analysis establishes that the controller only asserts a session and then writes the estado and a Facturacion row keyed by the supplied id with no facturacion permission or owner predicate."
    },
    "remediation": {
      "strategy": "Require facturacion:view (or admin:full, optionally validated against the DB) in ordenPago, and verify that the solicitud belongs to the caller when the caller is a customer; alternatively move the action under /api/facturacion.",
      "code_changes": [
        {
          "file_name": "src/controllers/solicitudes.controller.ts",
          "fixed_code": "async ordenPago(request: Request): Promise<NextResponse> {\n    const session = await getSession();\n    if (!session) return error(API_ERROR_CODES.UNAUTHORIZED, \"No autorizado\", 401);\n    if (!session.permissions.includes(\"facturacion:view\") && !session.permissions.includes(\"admin:full\")) {\n      return error(API_ERROR_CODES.FORBIDDEN, \"Sin permisos de facturacion\", 403);\n    }\n    const raw = await request.json() as { solicitudId: string };\n    ...\n  }"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "A single POST from any customer session is enough; no billing role is required."
      },
      "impact": {
        "score": "medium",
        "reason": "Integrity of the billing pipeline is compromised (arbitrary solicitudes moved to pending payment and invoices created), though no direct funds are moved by this call."
      },
      "overall_severity": "medium"
    },
    "confidence": {
      "score": "high",
      "reason": "The controller check, repository writes and the contrasting facturacion:view gate are all source-visible."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "solicitudes.reevaluar.missing-owner-and-permission",
    "title": "POST /api/solicitudes/reevaluar lets any solicitudes:view holder create a re-evaluation on any solicitud",
    "description": "solicitudesController.reevaluar checks only for a session, then verifies there is no pending re-evaluation and calls repo.crearReevaluacion with the attacker-supplied solicitudId, motivo and documentos (solicitudes.controller.ts:106-119). There is no ownership check, no permission check beyond solicitudes:view, and no existence check on the solicitud beyond a foreign-key write. The stored documentos are later copied into Solicitud.documentos on approval (solicitudes-repository.ts:310-312), so a customer can inject arbitrary document URLs and workflow state into another owner's solicitud.",
    "root_cause": "The re-evaluation creation path forwards the caller-supplied solicitudId, motivo and documentos straight to the repository, which creates the row keyed only on that id, with no session-derived owner/permission binding.",
    "intended_behavior": "Creating a re-evaluation should be restricted to the solicitud's owner (session.sub) or an entitled reviewer, and should validate the target's existence and state.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/solicitudes/[...slug]/route.ts",
        "line": 14,
        "scope": "POST reevaluar dispatch",
        "description": "Slug router maps POST /api/solicitudes/reevaluar to solicitudesController.reevaluar."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 115,
        "scope": "reevaluar",
        "description": "await services.solicitudes.repo.crearReevaluacion(raw.solicitudId, PENDIENTE, raw.motivo ?? null, raw.documentos ?? [], session.email) — only the session email is used; no ownership/permission check."
      },
      {
        "kind": "propagation",
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 286,
        "scope": "crearReevaluacion",
        "description": "prisma.reevaluacion.create({ data: { solicitudId, estado, motivo, documentos, createdBy } }) writes for any existing solicitud id."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 311,
        "scope": "atenderReevaluacionAprobacion",
        "description": "On approval the attacker-supplied documentos array is copied into solicitud.documentos, persisting it into the victim record."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 28,
        "description": "The /api/solicitudes prefix requires only solicitudes:view."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 212,
        "description": "The `cliente` role holds solicitudes:view, so customers can call reevaluar."
      },
      {
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 112,
        "description": "The only gate besides the session is tieneReevaluacionPendiente; no owner or area check is present."
      },
      {
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 287,
        "description": "documentos is stored as caller-supplied JSON with no validation."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "Any session with solicitudes:view is sufficient."
      },
      {
        "kind": "data_state",
        "description": "The target solicitud must exist and not already have a pending re-evaluation."
      }
    ],
    "execution": {
      "attacker_perspective": "An authenticated customer who wants to tamper with another party's solicitud workflow or inject documents into it.",
      "payloads": [
        "POST /api/solicitudes/reevaluar {\"solicitudId\":\"<victimUuid>\",\"motivo\":\"re-evaluacion\",\"documentos\":[\"https://attacker.example/x.pdf\"]}"
      ],
      "instructions": [
        "Authenticate as any holder of solicitudes:view.",
        "Obtain a target solicitud UUID without a pending re-evaluation.",
        "POST /api/solicitudes/reevaluar and observe that the reevaluacion row is created for that solicitud with the caller as createdBy."
      ],
      "observed_result": "Source analysis establishes that the handler performs no owner/permission comparison and the repository creates a Reevaluacion linked to the supplied solicitudId, so any solicitudes:view holder can create re-evaluations for other owners."
    },
    "remediation": {
      "strategy": "Load the solicitud, require that either session.sub equals its userId or the caller holds an internal review/admin permission, and validate that the solicitud exists and is in a re-evaluable state before creating the reevaluacion.",
      "code_changes": [
        {
          "file_name": "src/controllers/solicitudes.controller.ts",
          "fixed_code": "const detalle = await services.solicitudes.detalle(raw.solicitudId);\n    if (!detalle) return error(API_ERROR_CODES.NOT_FOUND, \"Solicitud no encontrada\", 404);\n    const esRevisor = session.permissions.some((p) => p.startsWith(\"solicitudes:review:\") || p === \"admin:full\");\n    if (!esRevisor && detalle.userId !== session.sub) {\n      return error(API_ERROR_CODES.FORBIDDEN, \"No puedes re-evaluar solicitudes de otro usuario\", 403);\n    }"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "Reachable from any customer session with a known/enumerable solicitud UUID and a single POST."
      },
      "impact": {
        "score": "medium",
        "reason": "Allows cross-owner workflow tampering and injection of attacker-chosen document URLs into another party's solicitud record."
      },
      "overall_severity": "medium"
    },
    "confidence": {
      "score": "high",
      "reason": "Both the controller and the repository write path are fully readable and contain no owner/permission enforcement."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "solicitudes.revisar.missing-area-permission",
    "title": "POST /api/solicitudes/revisar accepts any area from any solicitudes:view holder, bypassing area-specific review permissions",
    "description": "solicitudesController.revisar performs only a session check and then writes the review supplied in the body (solicitudes.controller.ts:40-45). The validator constrains area to the three enum values but performs no permission check (solicitudes.validator.ts:15-20), and REVISION_AREA_PERMISSIONS (constants.ts:302-306), which maps each area to its solicitudes:review:<area> permission, is referenced only in the client component solicitud-review.tsx:44 to hide buttons. The middleware gate for /api/solicitudes is the broad solicitudes:view permission (middleware.ts:28), held by `cliente` (constants.ts:212). A customer can therefore POST an approval/rejection for any area of any solicitud, driving estado transitions (computeEstadoSolicitud), triggering SGC expediente creation when legal is approved (solicitudes-service.ts:43-45) and generating reviewer alerts.",
    "root_cause": "The server trusts the area field and the mere presence of a session; the area-to-permission map exists but is never enforced on the write path, so any solicitudes:view holder can act as any review area.",
    "intended_behavior": "Recording a review must require the permission mapped to the submitted area (REVISION_AREA_PERMISSIONS[area] or admin:full) so that, for example, only a legal reviewer can approve the legal step.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/solicitudes/[...slug]/route.ts",
        "line": 11,
        "scope": "POST revisar dispatch",
        "description": "Slug router maps POST /api/solicitudes/revisar to solicitudesController.revisar."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 41,
        "scope": "revisar",
        "description": "const session = await getSession(); the session is only checked for existence — no hasPermission/role check against the submitted area."
      },
      {
        "kind": "propagation",
        "file": "src/validators/solicitudes.validator.ts",
        "line": 17,
        "scope": "solicitudesRevisarSchema",
        "description": "area is zod.enum(REVISION_AREAS) and estado is zod.enum(RESULTADOS_APROBACION); the attacker chooses both freely."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 224,
        "scope": "crearOActualizarRevision",
        "description": "prisma.revision.update/create persists the chosen estado for the chosen area, recording the caller email as the reviewer, with no authorization predicate."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 28,
        "description": "Only solicitudes:view gates /api/solicitudes, so any customer passes."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 302,
        "description": "REVISION_AREA_PERMISSIONS defines solicitudes:review:comunicacion / legal / logistica, but no server file imports/references it."
      },
      {
        "file": "src/components/solicitudes/solicitud-review.tsx",
        "line": 44,
        "description": "The only reference to REVISION_AREA_PERMISSIONS is client-side, proving the permission is enforced only in the UI."
      },
      {
        "file": "src/application/solicitudes/solicitudes-service.ts",
        "line": 43,
        "description": "A legal approval triggers sgcIntegracion.crearExpedienteDesdeSolicitud, showing the business impact of unauthorized area actions."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 212,
        "description": "The `cliente` role holds solicitudes:view, hence the reachable caller includes customers."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "Any valid session carrying solicitudes:view is sufficient; no area permission is checked."
      },
      {
        "kind": "authorization_role",
        "description": "The intended per-area permissions (solicitudes:review:*) are never required by the server."
      }
    ],
    "execution": {
      "attacker_perspective": "An authenticated customer or any user holding only solicitudes:view who wants to force approval/rejection of a solicitud.",
      "payloads": [
        "POST /api/solicitudes/revisar {\"solicitudId\":\"<uuid>\",\"area\":\"legal\",\"estado\":\"aprobado\",\"comentario\":\"ok\"}",
        "POST /api/solicitudes/revisar {\"solicitudId\":\"<uuid>\",\"area\":\"logistica\",\"estado\":\"rechazado\"}"
      ],
      "instructions": [
        "Authenticate as a holder of solicitudes:view (e.g. the `cliente` role).",
        "POST /api/solicitudes/revisar with a target solicitudId and any area/estado combination.",
        "Observe that the revision is recorded under the caller's email and the solicitud state/alerts/SGC flow advance."
      ],
      "observed_result": "Source analysis establishes that after the getSession presence check the submitted area/estado are persisted by crearOActualizarRevision with the caller as reviewer and no permission comparison, so any solicitudes:view holder can act for any area."
    },
    "remediation": {
      "strategy": "After parsing, require the permission mapped to body.area (REVISION_AREA_PERMISSIONS[body.area]) or admin:full before calling services.solicitudes.revisar; enforce the same check against the database permission source for critical transitions.",
      "code_changes": [
        {
          "file_name": "src/controllers/solicitudes.controller.ts",
          "fixed_code": "const body = solicitudesRevisarSchema.parse(await request.json());\n    const required = REVISION_AREA_PERMISSIONS[body.area as keyof typeof REVISION_AREA_PERMISSIONS];\n    if (!session.permissions.includes(required) && !session.permissions.includes(\"admin:full\")) {\n      return error(API_ERROR_CODES.FORBIDDEN, \"Sin permiso para revisar esta area\", 403);\n    }\n    return success(await services.solicitudes.revisar({ ...body, reviewerEmail: session.email }));"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "Trivial to invoke with any customer session and a single POST; no knowledge of internal roles is required."
      },
      "impact": {
        "score": "high",
        "reason": "Unauthorized approvals/rejections corrupt the review workflow, record the attacker as reviewer, and can trigger downstream SGC contract-expediente creation."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "The missing check and the unused permission map are both fully visible in source."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "solicitudes.upload-doc.missing-owner-binding",
    "title": "POST /api/solicitudes/upload-doc attaches a document to any solicitud id without owner binding",
    "description": "solicitudesController.uploadDocumento requires a session and then calls services.solicitudes.uploadDocumento with the caller-supplied solicitudId and url (solicitudes.controller.ts:200-213). The application service computes only whether the caller is admin/upload and sets the new document's userId accordingly, then calls repo.crearDocumentoAdjunto without ever verifying that the target solicitud belongs to the caller (solicitudes-service.ts:84-98), and the repository creates the row keyed only on the supplied solicitudId (solicitudes-repository.ts:413-417). The sibling modificar path (controller.ts:94) and eliminarDocumento (service.ts:105-110) do enforce ownership, demonstrating the intended invariant. Any `cliente` (constants.ts:212) can therefore attach arbitrary document URLs to another customer's solicitud.",
    "root_cause": "uploadDocumento authorizes only that a session exists and derives the document's userId from the caller, but never checks the parent solicitud's owner (or the caller's reviewer scope) before creating the child record.",
    "intended_behavior": "The service must load the solicitud and deny the write unless the caller is the owner (session.sub equals Solicitud.userId) or holds an internal upload/review permission, mirroring the checks used by modificar and eliminarDocumento.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/solicitudes/[...slug]/route.ts",
        "line": 18,
        "scope": "POST upload-doc dispatch",
        "description": "Slug router maps POST /api/solicitudes/upload-doc to solicitudesController.uploadDocumento."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 206,
        "scope": "uploadDocumento",
        "description": "The controller forwards raw.solicitudId and raw.url to services.solicitudes.uploadDocumento after only a session presence check; it never loads the solicitud."
      },
      {
        "kind": "propagation",
        "file": "src/application/solicitudes/solicitudes-service.ts",
        "line": 92,
        "scope": "uploadDocumento",
        "description": "isAdmin is computed from permissions and used only to null out the document's userId; the solicitudId is passed to the repository unverified."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/persistence/solicitudes-repository.ts",
        "line": 414,
        "scope": "crearDocumentoAdjunto",
        "description": "prisma.solicitudDocumento.create({ data: { solicitudId, url, nombre, uploadedBy, userId } }) inserts the document into any solicitud id."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 28,
        "description": "Only solicitudes:view gates /api/solicitudes, so any customer can call upload-doc."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 212,
        "description": "The `cliente` role holds solicitudes:view."
      },
      {
        "file": "src/application/solicitudes/solicitudes-service.ts",
        "line": 107,
        "description": "eliminarDocumento enforces doc.userId === userSub (or admin), showing that ownership binding is expected in this feature."
      },
      {
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 94,
        "description": "modificar enforces the owner check that uploadDocumento omits."
      }
    ],
    "conditions": [
      {
        "kind": "authentication_level",
        "description": "Any session with solicitudes:view suffices."
      },
      {
        "kind": "data_state",
        "description": "A target solicitud id must exist; ids are enumerable through the unscoped listar endpoint."
      }
    ],
    "execution": {
      "attacker_perspective": "An authenticated customer who wants to attach documents to another company's solicitud or tamper with its file list.",
      "payloads": [
        "POST /api/solicitudes/upload-doc {\"solicitudId\":\"<victimUuid>\",\"url\":\"https://attacker.example/contracto.pdf\",\"nombre\":\"contrato.pdf\"}"
      ],
      "instructions": [
        "Authenticate as any holder of solicitudes:view.",
        "Obtain a target solicitud UUID.",
        "POST /api/solicitudes/upload-doc and then load the victim's solicitud detail to observe the injected document."
      ],
      "observed_result": "Source analysis establishes that the service never loads or owner-checks the solicitud and the repository inserts the SolicitudDocumento for the supplied id, so a document is attached to another owner's record."
    },
    "remediation": {
      "strategy": "In uploadDocumento, load the solicitud and require ownership (session.sub equals Solicitud.userId) unless the caller holds an internal upload/review permission; reject with FORBIDDEN/NOT_FOUND otherwise.",
      "code_changes": [
        {
          "file_name": "src/application/solicitudes/solicitudes-service.ts",
          "fixed_code": "async uploadDocumento(params: {...}): Promise<Record<string, unknown>> {\n    const sol = await this.repo.detalle(params.solicitudId);\n    if (!sol) throw new DomainError(\"Solicitud no encontrada\", API_ERROR_CODES.NOT_FOUND, 404);\n    const isAdmin = params.userPermissions.includes(\"admin:full\") || params.userPermissions.includes(\"solicitudes:upload\");\n    if (!isAdmin && sol.userId !== params.userSub) {\n      throw new DomainError(\"No puedes adjuntar documentos a solicitudes de otro usuario\", API_ERROR_CODES.FORBIDDEN, 403);\n    }\n    return this.repo.crearDocumentoAdjunto(params.solicitudId, params.url, params.nombre, isAdmin ? null : params.userSub, params.userEmail);\n  }"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "A single POST from any customer session attaches a document; target ids are enumerable."
      },
      "impact": {
        "score": "medium",
        "reason": "Cross-owner integrity violation of the document set used in the review workflow and shown to the owner and reviewers; it does not by itself expose data."
      },
      "overall_severity": "medium"
    },
    "confidence": {
      "score": "high",
      "reason": "The missing check and the contrast with the sibling ownership checks are entirely visible in source."
    }
  },
  {
    "verdict": "rejected",
    "fingerprint": "solicitudes.upload-doc.unvalidated-url-to-href",
    "title": "Stored document url is not scheme-validated before rendering as an anchor href",
    "description": "It is true that uploadDocumento stores the caller-supplied url with no application-level scheme validation and the client components render it directly as an anchor href (mis-solicitudes-manager.tsx:365,386,403,467 and cliente-upload-modal.tsx:82,96). However, the claimed DOM/stored-XSS boundary is not crossed: the anchors are React elements, and React DOM's production URL sanitizer replaces any javascript: URL with a non-executable javascript:throw statement before setting the href attribute (react-dom-client.production.js:1410-1416 and the href case at :12990-13005), no dangerouslySetInnerHTML sink exists in src, and modern browsers block top-level navigation to data: URLs. The deployed app is a production Next.js build (docker/entrypoint.sh:44 runs npm run build with NODE_ENV=production), so the production sanitizer is the effective render path.",
    "claimed_root_cause": "solicitudesService.uploadDocumento persists raw.url into SolicitudDocumento and the components bind doc.url/documentos entries to <a href> without a scheme allowlist, which the candidate treats as reachable script execution.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/app/api/solicitudes/[...slug]/route.ts",
        "line": 18,
        "scope": "POST upload-doc dispatch",
        "description": "The url field is accepted from the request body."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/solicitudes.controller.ts",
        "line": 208,
        "scope": "uploadDocumento",
        "description": "raw.url is stored unchanged; only presence is validated."
      },
      {
        "kind": "sink",
        "file": "src/components/solicitudes/mis-solicitudes-manager.tsx",
        "line": 365,
        "scope": "documentos href render",
        "description": "<a href={url}> renders the stored value, but through React's setAttribute path which sanitizes javascript: URLs in production."
      }
    ],
    "evidence": [
      {
        "file": "node_modules/react-dom/cjs/react-dom-client.production.js",
        "line": 1412,
        "description": "function sanitizeURL(url) { return isJavaScriptProtocol.test('' + url) ? \"javascript:throw new Error('React has blocked a javascript: URL as a security precaution.')\" : url; }"
      },
      {
        "file": "node_modules/react-dom/cjs/react-dom-client.production.js",
        "line": 13004,
        "description": "The href attribute case calls value = sanitizeURL('' + value) before domElement.setAttribute, so javascript: hrefs are neutralized."
      },
      {
        "file": "node_modules/react-dom/cjs/react-dom-server.node.production.js",
        "line": 285,
        "description": "The server renderer applies the same isJavaScriptProtocol check, covering the SSR pass of the client components."
      },
      {
        "file": "src/components/solicitudes/mis-solicitudes-manager.tsx",
        "line": 633,
        "description": "The only other URL sinks are <img src={...}> for imagenes, not the stored document url, and the rendered attribute value is HTML-attribute-escaped by React."
      },
      {
        "file": "docker/entrypoint.sh",
        "line": 44,
        "description": "First-run deployment executes npm run build, producing a production React bundle that contains the blocking sanitizer."
      }
    ],
    "reason": "Refuted by a visible framework control: React DOM's production sanitizer converts javascript: hrefs to a throwing javascript: URL before setting the attribute (react-dom-client.production.js:1412-1416), there is no dangerouslySetInnerHTML or other raw-HTML sink in src, and browsers block top-level data: navigation. In a production build the stored url therefore cannot become an executable scheme, so the claimed XSS boundary violation does not hold; the residual is at most a non-executable phishing/open-redirect link, which is not the asserted finding."
  },
  {
    "verdict": "needs_validation",
    "fingerprint": "sunat.ruc.unauth-operator-credential-pii-lookup",
    "title": "Unauthenticated SUNAT RUC lookup under the server-side operator token",
    "description": "GET /api/sunat/ruc validates only the 11-digit format and then calls the external provider api.apis.net.pe with a server-side Bearer token. The route is absent from both PROTECTED and the public allowlists, so middleware lets anonymous callers through, and the controller never establishes a session. The claimed consequence is that an unauthenticated caller can drive operator-credentialed RUC lookups and read returned business/PII data; that consequence depends on provider and deployment facts that source does not establish.",
    "claimed_root_cause": "src/middleware.ts:50-72 lets /api/sunat through because no PROTECTED prefix matches; consultas.controller.ts:7-19 checks only an 11-digit pattern and calls consultasClient.consultarRuc, which in consultas-client.ts:9-10 attaches Authorization: Bearer ${SUNAT_API_TOKEN} to the provider request.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 50,
        "scope": "PROTECTED prefix loop",
        "description": "No PROTECTED entry prefix-matches /api/sunat and it is not in the PUBLIC lists, so the request passes through to the route."
      },
      {
        "kind": "propagation",
        "file": "src/app/api/sunat/[...slug]/route.ts",
        "line": 6,
        "scope": "GET ruc dispatch",
        "description": "createRouter dispatches the anonymous GET to sunatController.consultarRuc."
      },
      {
        "kind": "propagation",
        "file": "src/controllers/consultas.controller.ts",
        "line": 10,
        "scope": "consultarRuc format check",
        "description": "Only /^\\d{11}$/ is enforced; no getSession or permission check precedes the provider call."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/external/consultas-client.ts",
        "line": 9,
        "scope": "fetchAPI provider request",
        "description": "Issues GET to the fixed RUC URL with Authorization: Bearer ${SUNAT_API_TOKEN}; with the token unset the function throws before calling the provider."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 72,
        "description": "Unmatched paths fall through to NextResponse.next()."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 30,
        "description": "PUBLIC_API_ROUTES does not include /api/sunat."
      },
      {
        "file": "src/controllers/consultas.controller.ts",
        "line": 10,
        "description": "Only an 11-digit pattern is validated; no session check."
      },
      {
        "file": "src/infrastructure/external/consultas-client.ts",
        "line": 3,
        "description": "The operator token is read from SUNAT_API_TOKEN."
      },
      {
        "file": "src/infrastructure/external/consultas-client.ts",
        "line": 21,
        "description": "consultarRuc targets the fixed api.apis.net.pe RUC URL."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "Whether SUNAT_API_TOKEN is provisioned in the deployed task is not source-visible; consultas-client.ts:8 throws before any provider call when it is unset.",
      "The provider response (whether api.apis.net.pe returns full business/PII data) and the provider-side authorization are external facts not present in the repository."
    ],
    "validation_plan": {
      "local": "In an OS-enforced sandbox with an allowlisted empty environment, run the /api/sunat/ruc handler against a loopback stub for api.apis.net.pe using a dummy token and assert that an anonymous request with no Cookie reaches the handler and issues the Bearer request; use no external network.",
      "deployment": "An owner confirms whether SUNAT_API_TOKEN is configured and observes that an unauthenticated GET /api/sunat/ruc?numero=<dummy 11 digits> from outside returns provider data (or 502 when the token is unset) with no token cookie present."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "terraform.cloudfront:origin-http-only-cleartext",
    "title": "CloudFront forwards viewer traffic to the ALB origin over cleartext HTTP",
    "description": "The CloudFront distribution's custom origin sets origin_protocol_policy = \"http-only\", so CloudFront terminates TLS at the edge and relays requests to the internet-facing ALB over plaintext HTTP on port 80, exposing session JWTs, PII and payment-related data to interception between CloudFront and the origin despite an available ALB HTTPS listener.",
    "root_cause": "The origin protocol policy is hardcoded to http-only even though the ALB provisions an HTTPS listener when enable_https is true (it is true in prod).",
    "intended_behavior": "CloudFront should reach the origin over HTTPS (origin_protocol_policy = \"https-only\") using the ALB's TLS listener and a validated certificate.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "terraform/modules/cloudfront/main.tf",
        "line": 184,
        "scope": "aws_cloudfront_distribution.main origin",
        "description": "Origin is the internet-facing ALB DNS name."
      },
      {
        "kind": "propagation",
        "file": "terraform/modules/cloudfront/main.tf",
        "line": 188,
        "scope": "custom_origin_config",
        "description": "http_port 80 / https_port 443 declared, protocol then forced to http-only."
      },
      {
        "kind": "sink",
        "file": "terraform/modules/cloudfront/main.tf",
        "line": 191,
        "scope": "custom_origin_config.origin_protocol_policy",
        "description": "origin_protocol_policy = \"http-only\" — CloudFront uses port 80 to the origin."
      }
    ],
    "evidence": [
      {
        "file": "terraform/modules/cloudfront/main.tf",
        "line": 191,
        "description": "Explicit http-only origin protocol despite viewer_protocol_policy = redirect-to-https for clients."
      },
      {
        "file": "terraform/modules/ecs/main.tf",
        "line": 550,
        "description": "An ALB HTTPS listener exists (enable_https true in prod), so the cleartext origin hop is a choice, not a constraint."
      },
      {
        "file": "terraform/modules/ecs/main.tf",
        "line": 534,
        "description": "Comments acknowledge the http-only origin design (ALB must forward, not redirect, to avoid a loop)."
      }
    ],
    "conditions": [
      {
        "kind": "network_routing",
        "description": "CloudFront connects to the ALB's public DNS over port 80, so the hop crosses networks where plaintext HTTP can be observed."
      }
    ],
    "execution": {
      "attacker_perspective": "Network-positioned adversary (or a compromised intermediate) on the path between a CloudFront edge and the ALB/region.",
      "payloads": [
        "Passive capture of plaintext HTTP requests/responses on the CloudFront-to-ALB hop."
      ],
      "instructions": [
        "Static reproduction (no target execution performed): read terraform/modules/cloudfront/main.tf:191 and confirm origin_protocol_policy = \"http-only\".",
        "Deployment-time reproduction: inspect the deployed origin's traffic and confirm requests arrive at the ALB port 80 without TLS."
      ],
      "observed_result": "Source fixes the origin protocol to HTTP-only, so edge-to-origin payloads are transported unencrypted."
    },
    "remediation": {
      "strategy": "Set the origin protocol policy to https-only and point CloudFront at the ALB's TLS listener with the validated certificate.",
      "code_changes": [
        {
          "file_name": "terraform/modules/cloudfront/main.tf",
          "fixed_code": "    custom_origin_config {\n      http_port              = 80\n      https_port             = 443\n      origin_protocol_policy = \"https-only\"\n      origin_ssl_protocols   = [\"TLSv1.2\"]\n    }"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "medium",
        "reason": "Requires a network position on the CloudFront-to-origin path; the hop is not authenticated by mutual TLS."
      },
      "impact": {
        "score": "medium",
        "reason": "Interception reveals session tokens, PII and commercial data in transit, but the attacker cannot trivially rewrite responses on an encrypted viewer leg."
      },
      "overall_severity": "medium"
    },
    "confidence": {
      "score": "high",
      "reason": "The origin protocol policy is an explicit literal in the distribution resource; no deployment observation is needed to establish cleartext origin traffic."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "terraform.ecs:alb-origin-public-bypasses-waf",
    "title": "Internet-facing ALB is reachable directly and bypasses the CloudFront WAF",
    "description": "WAF is attached only to the CloudFront distribution, but the ALB security group allows 0.0.0.0/0 on ports 80 and 443 because restrict_alb_to_cloudfront defaults to false and the prod deployment profile does not enable it. The ALB is internal=false with public subnets, so clients can hit the origin directly and skip the CloudFront managed rules and IP rate limit.",
    "root_cause": "ALB ingress is opened to all IPs while the WAF is bound solely to CloudFront; the CloudFront-origin-facing prefix-list control is opt-in (default false) and absent from prod.tfvars and the valle/evento profiles.",
    "intended_behavior": "ALB ingress should be restricted to the AWS CloudFront origin-facing managed prefix list so all internet traffic traverses the WAF-protected distribution.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "terraform/main.tf",
        "line": 201,
        "scope": "module.ecs invocation",
        "description": "restrict_alb_to_cloudfront is wired from the variable that defaults to false."
      },
      {
        "kind": "propagation",
        "file": "terraform/modules/ecs/main.tf",
        "line": 461,
        "scope": "aws_security_group.alb ingress",
        "description": "cidr_blocks = var.restrict_alb_to_cloudfront ? [] : [\"0.0.0.0/0\"] — publicly open by default."
      },
      {
        "kind": "sink",
        "file": "terraform/modules/cloudfront/main.tf",
        "line": 182,
        "scope": "aws_cloudfront_distribution.main",
        "description": "web_acl_id attaches the WAF to CloudFront only, so direct ALB requests are unfiltered."
      }
    ],
    "evidence": [
      {
        "file": "terraform/variables.tf",
        "line": 367,
        "description": "restrict_alb_to_cloudfront default = false."
      },
      {
        "file": "terraform/prod.tfvars",
        "line": 9,
        "description": "Prod var file does not set restrict_alb_to_cloudfront, and its documented usage combines prod.tfvars with perfiles/valle.tfvars."
      },
      {
        "file": "terraform/perfiles/valle.tfvars",
        "line": 1,
        "description": "The valle profile (documented prod profile) never sets restrict_alb_to_cloudfront, leaving the default false."
      },
      {
        "file": "terraform/modules/ecs/main.tf",
        "line": 498,
        "description": "aws_lb.main internal = false with public subnets, so the ALB is internet-facing."
      },
      {
        "file": "terraform/modules/ecs/main.tf",
        "line": 474,
        "description": "HTTPS ingress also opens 0.0.0.0/0 when the restriction is off."
      },
      {
        "file": "terraform/perfiles/apertura.tfvars",
        "line": 25,
        "description": "Only the apertura profile sets restrict_alb_to_cloudfront = true, showing the control exists but is not the default prod state."
      }
    ],
    "conditions": [
      {
        "kind": "network_routing",
        "description": "The ALB resides in public subnets and is internet-facing, so 0.0.0.0/0 ingress is directly reachable."
      },
      {
        "kind": "system_configuration",
        "description": "The prod plan artifact (terraform/plan-prod.json) shows the ALB security group with 0.0.0.0/0 on 80 and 443 and description 'HTTP publico'."
      }
    ],
    "execution": {
      "attacker_perspective": "Unauthenticated internet client that knows or discovers the ALB DNS name.",
      "payloads": [
        "curl -sk https://<alb-dns-name>/api/...  # direct to origin, no WAF",
        "High-rate request flood against the ALB to avoid the WAF IP rate-based rule."
      ],
      "instructions": [
        "Static reproduction (no target execution performed): read terraform/variables.tf:367 (default false), terraform/modules/ecs/main.tf:457-476 (0.0.0.0/0 when off), and terraform/modules/cloudfront/main.tf:182 (WAF on CloudFront only).",
        "Inspect terraform/plan-prod.json: the module.ecs.aws_security_group.alb ingress lists 0.0.0.0/0 on 80 and 443 with description 'HTTP publico'.",
        "Deployment-time reproduction: request the ALB DNS directly and observe responses that never traversed CloudFront/WAF."
      ],
      "observed_result": "Source and the prod plan show the ALB opened to 0.0.0.0/0 while WAF guards only CloudFront, so direct origin requests bypass all WAF rules."
    },
    "remediation": {
      "strategy": "Enable restrict_alb_to_cloudfront in the production profiles and set the default to true, so ALB ingress is limited to the CloudFront origin-facing prefix list.",
      "code_changes": [
        {
          "file_name": "terraform/variables.tf",
          "fixed_code": "variable \"restrict_alb_to_cloudfront\" {\n  description = \"Permitir ingreso al ALB SOLO desde los rangos de CloudFront (prod). En qa = false para poder probar el ALB directo\"\n  type        = bool\n  default     = true\n}"
        },
        {
          "file_name": "terraform/prod.tfvars",
          "fixed_code": "restrict_alb_to_cloudfront = true"
        }
      ]
    },
    "severity": {
      "likelihood": {
        "score": "high",
        "reason": "Direct ALB access requires only the origin DNS name, which is resolvable/discoverable, and the default/documented prod profile leaves it open."
      },
      "impact": {
        "score": "high",
        "reason": "Bypassing the WAF removes OWASP managed rules and the IP rate limit, enabling direct exploitation and volumetric abuse against the origin app, including the unprotected endpoints already found in this audit."
      },
      "overall_severity": "high"
    },
    "confidence": {
      "score": "high",
      "reason": "The conditional ingress, the default value, the prod profiles and the prod plan artifact all agree that the ALB is publicly reachable and WAF-unguarded."
    }
  },
  {
    "verdict": "confirmed",
    "fingerprint": "tls.external-clients.cert-verification-disabled",
    "title": "Outbound integration clients disable TLS certificate verification process-wide",
    "description": "Both the KBServicios and Planogess clients set process.env.NODE_TLS_REJECT_UNAUTHORIZED=\"0\" immediately before fetch and reset it to \"1\" afterwards. The toggle is process-global and covers every TLS connection made during the window; it is skipped on any thrown fetch (no try/finally), leaving verification disabled until another call resets it. The configured endpoints are HTTPS.",
    "root_cause": "The clients mutate the global NODE_TLS_REJECT_UNAUTHORIZED environment flag instead of configuring verification per connection, and they are singletons in services.ts, so any in-flight or failed call runs with peer verification disabled.",
    "intended_behavior": "Outbound TLS clients must verify the peer certificate; any relaxation must be an explicit, per-connection configuration, never a process-global toggle.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/infrastructure/external/kbservicios-client.ts",
        "line": 7,
        "scope": "fetchApi",
        "description": "Sets process.env.NODE_TLS_REJECT_UNAUTHORIZED=\"0\" before the outbound call."
      },
      {
        "kind": "propagation",
        "file": "src/infrastructure/external/kbservicios-client.ts",
        "line": 8,
        "scope": "fetchApi -> fetch(BASE_URL)",
        "description": "Performs the HTTPS request with verification disabled process-wide."
      },
      {
        "kind": "sink",
        "file": "src/infrastructure/external/planogess-client.ts",
        "line": 34,
        "scope": "PlanogessClient.fetchStands",
        "description": "Same global toggle; the reset at line 34 is unreachable if the fetch at line 28 throws."
      }
    ],
    "evidence": [
      {
        "file": "src/infrastructure/external/kbservicios-client.ts",
        "line": 7,
        "description": "Disables certificate verification before fetch."
      },
      {
        "file": "src/infrastructure/external/kbservicios-client.ts",
        "line": 14,
        "description": "Re-enables verification only after a successful fetch (no finally)."
      },
      {
        "file": "src/infrastructure/external/planogess-client.ts",
        "line": 27,
        "description": "Repeats the global disable for the Planogess client."
      },
      {
        "file": "src/lib/server/services.ts",
        "line": 29,
        "description": "Both clients are constructed as module-level singletons shared process-wide."
      },
      {
        "file": ".env.prod.example",
        "line": 25,
        "description": "PLANOGESS_API_URL and KBSERVICIOS_URL point at HTTPS endpoints (https://secure2.iimp.org:8443)."
      }
    ],
    "conditions": [
      {
        "kind": "system_configuration",
        "description": "The external endpoints are HTTPS (secure2.iimp.org:8443)."
      },
      {
        "kind": "network_routing",
        "description": "An attacker can intercept or redirect the app's outbound connection to those endpoints."
      }
    ],
    "execution": {
      "attacker_perspective": "An on-path network attacker between the application host and secure2.iimp.org.",
      "payloads": [
        "Serve a self-signed or otherwise invalid certificate for secure2.iimp.org:8443"
      ],
      "instructions": [
        "Position on the network path of the KBServicios/Planogess calls.",
        "Present a certificate that would normally fail validation and return a forged response."
      ],
      "observed_result": "Source analysis shows the clients set NODE_TLS_REJECT_UNAUTHORIZED=\"0\" around the fetch, so peer certificate verification is disabled for that connection and a tampered response is accepted."
    },
    "remediation": {
      "strategy": "Remove the global NODE_TLS_REJECT_UNAUTHORIZED toggle and keep certificate verification enabled; if a legacy endpoint uses a private CA, pin that CA per-request or per-agent instead of disabling verification globally."
    },
    "severity": {
      "likelihood": {
        "score": "low",
        "reason": "Exploitation requires a network position on the outbound path to the integration servers."
      },
      "impact": {
        "score": "high",
        "reason": "Man-in-the-middle tampering can alter event/stand/planogess data ingested by the application without detection."
      },
      "overall_severity": "medium"
    },
    "confidence": {
      "score": "high",
      "reason": "The env toggle, its scope, the missing finally and the HTTPS endpoints are all directly visible in source."
    }
  },
  {
    "verdict": "needs_validation",
    "fingerprint": "upload/unauth-extension-to-public-html",
    "title": "Unauthenticated file upload with attacker-controlled extension into the public web root",
    "description": "POST /api/upload accepts a multipart file with no authentication, derives the stored extension from the caller-supplied file name without an allowlist, and the default storage adapter writes the bytes into public/uploads. nginx serves /uploads/ from the same origin with extension-inferred content type and no nosniff or Content-Disposition. Depending on the active storage provider and serving layer, an anonymous caller can place active content (for example .html or .svg) that a victim's browser executes in the application origin.",
    "claimed_root_cause": "src/middleware.ts leaves /api/upload ungated; src/app/api/upload/route.ts:17 derives the extension from file.name with no allowlist; src/lib/server/storage.ts:12-16 writes to public/uploads by default; docker/nginx.conf:26-30 serves /uploads/ with an extension-based content type and no nosniff or attachment header.",
    "trace": [
      {
        "kind": "entrypoint",
        "file": "src/middleware.ts",
        "line": 50,
        "scope": "PROTECTED prefix loop",
        "description": "/api/upload matches no PROTECTED entry and is not public, so the anonymous request passes through."
      },
      {
        "kind": "propagation",
        "file": "src/app/api/upload/route.ts",
        "line": 7,
        "scope": "POST handler",
        "description": "Reads formData(file) and processes the upload with no session check."
      },
      {
        "kind": "propagation",
        "file": "src/lib/server/storage.ts",
        "line": 11,
        "scope": "localAdapter.upload",
        "description": "Writes the buffer to <cwd>/public/uploads/<timestamp>_<random>.<ext>, where ext came from the attacker file name."
      },
      {
        "kind": "sink",
        "file": "docker/nginx.conf",
        "line": 26,
        "scope": "location /uploads/",
        "description": "Serves the stored file from /app/public with the mime type inferred from the extension and no X-Content-Type-Options or Content-Disposition."
      }
    ],
    "evidence": [
      {
        "file": "src/middleware.ts",
        "line": 12,
        "description": "No PROTECTED entry matches /api/upload."
      },
      {
        "file": "src/lib/shared/constants.ts",
        "line": 30,
        "description": "PUBLIC_API_ROUTES does not include /api/upload."
      },
      {
        "file": "src/app/api/upload/route.ts",
        "line": 17,
        "description": "Extension is file.name.split('.').pop() with no allowlist."
      },
      {
        "file": "src/app/api/upload/route.ts",
        "line": 19,
        "description": "Content type comes from file.type for the S3 branch."
      },
      {
        "file": "src/lib/server/storage.ts",
        "line": 12,
        "description": "Default adapter writes to public/uploads."
      },
      {
        "file": "src/lib/server/storage.ts",
        "line": 52,
        "description": "Provider is local unless STORAGE_PROVIDER=s3."
      },
      {
        "file": "docker/nginx.conf",
        "line": 27,
        "description": "Only expires and Cache-Control are set; no X-Content-Type-Options or Content-Disposition."
      },
      {
        "file": "docker-compose.prod.yml",
        "line": 52,
        "description": "The host repo is bind-mounted rw at /app so public/uploads persists and is served."
      }
    ],
    "blockers": [
      "no OS-enforced sandbox available on the audit host to execute target-controlled code",
      "The deployed STORAGE_PROVIDER (local vs s3) is not source-visible; the S3 branch returns a bucket/CDN URL and hard-codes public-read, which changes the origin from the app host to the bucket.",
      "Which serving layer fronts the app in the active deployment (EC2 nginx /app/public, Next static serving of public/, or CloudFront/S3) and therefore the exact response Content-Type are deployment facts not fully established by source."
    ],
    "validation_plan": {
      "local": "In an OS-enforced sandbox, invoke the POST handler with a bounded multipart fixture whose filename ends in .html and assert the returned path; then request it through the configured static handler and record the Content-Type and the absence of nosniff. Use no external network.",
      "deployment": "An owner confirms the deployed STORAGE_PROVIDER and observes that GET https://<host>/uploads/<random>.html returns Content-Type text/html with the uploaded bytes and without X-Content-Type-Options: nosniff or Content-Disposition: attachment."
    }
  }
]